# 高血池普通战翻盘复核

1197-F23 暴露了一个边界：`LOUSE_PROGENITOR` 虽是 Normal，却有 134~136 初始
血量，并能通过 CurlAndGrow 获得格挡和力量。战斗端先判定斩杀竞速失败，静态
`_race_joint_feasible` 再以可行攻防分配重开防守线；旧翻盘比上限只覆盖 Boss。

本次把同一约束扩展到当前血池达到 `power_commit_pool_min` 的 Monster/Elite，
新增 `longfight_race_joint_flip_max_ttk_ratio` 配置（默认 1.5，置 0 回滚）和
`LONGFIGHT_JOINT_FLIP_TTK_CAP` 观测标记。低血池与 Boss 原路径保持独立。selfcheck
验证了开启、关闭和低血池三态，结果为 `SELFCHECK OK`。

后续记录标记触发时的血池、TTK/生存比、出牌、战损与胜负；若连续 3 个高血池样本
没有改善，或低血池误触发，先关闭该键并依据完整决策链收窄条件。

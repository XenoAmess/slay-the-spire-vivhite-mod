# BOSS_SETTLE_TIER3 复盘经验

本批复核确认，结算等待并非普通的无牌可出：Boss 层已有 15 个独立对局、21 个样本，其中
17 个在深预算内没有重新开放出牌接口，且 4 个样本只剩 1 点能量。此前深预算以 energy >= 2
门控，导致真实可出牌窗口被提前收口。

本次把 Boss 层的 latent 结算窗口单独放宽到 40 tick；基准预算关闭、latent 为空、非 Boss 层和
运行时键置 0 均保持旧语义。自检增加默认值、回滚和非 Boss 边界夹具。后续应按独立对局统计
Boss 收口率、40 tick 内重开率和实际锁窗时长，再决定是否继续调整。

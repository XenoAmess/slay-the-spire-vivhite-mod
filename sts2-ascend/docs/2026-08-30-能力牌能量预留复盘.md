# 2026-08-30｜能力牌能量预留复盘

1189-F17-T1 暴露了一个具体缺口：`reserve_for_block` 已约束攻击牌，却没有进入能力/增益牌的长战评分。战栗、杂耍、好勇斗狠连续消耗 3 点能量，7 点来袭意图下留下 0 格挡。

本次在 `policy.py::_score_play` 增加最小预留闸门：非致死、非败局竞速时，能力牌会消耗最后预留能量且已有合格格挡时，压到禁玩线并留下“能量预留给格挡”观测；仍能同时支付能力与格挡，或没有有效格挡时保持原评分。`selfcheck.py` 覆盖能量 1/2 点和端到端选防御。

验证：`py -3 -B sts2-ascend/brain/selfcheck.py` → `SELFCHECK OK`。后续观察 3~10 局的标记、incoming、格挡兑现和 F17 早期战损；若标记与不可用格挡错配，或早期战损不降而能力窗口显著减少，回退该分支。

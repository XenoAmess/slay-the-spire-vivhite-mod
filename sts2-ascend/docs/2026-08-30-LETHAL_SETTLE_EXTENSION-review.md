# 2026-08-30｜致死 Boss 结算窗口复盘

## 可证伪假设

第 1085 局 F17-T10 的 `end_turn` 主要由结算窗口过早收口造成：玩家仅剩 8 HP、0 格挡，敌方即将造成 18 点伤害；能量为 3，四张牌的费用和目标规则仍可满足，但 payload 将它们全部标为不可用。若把“Boss 且本次伤害致死”的结算等待从 40 tick 有界延长到 50 tick，未来 3—10 局应出现 `lethal=yes`，并可能在第 40—50 tick 间重新观察到可执行牌；若仍无牌，则该假设转向 payload/API 锁窗问题。

## 证据与生产改动

- 失败运行 `7Z74W2SW09H3`（第 1085 局）决策 #280：F17-T10，8/80 HP、0 格挡、18 incoming、3 能量；`play_card` 牌的费用/目标合法，但 payload 的 `playable=false`，最终以预算 40 收口。
- 生产策略仅在既有 Boss 结算分支、仍有 latent 合法牌且已判定致死时，将预算提升至配置的 `end_turn_settle_recovery_ticks_lethal`（默认 50）；非 Boss、非致死和没有 latent 牌的路径不变。
- 观测理由追加 `lethal=yes|no`，便于把“等待不足”和“供给/API 不可用”区分开；没有改变评分、排序或普通回合动作。
- 失败回放包已核对：目标包是纯报告，历史尝试中的有效候选均归因于本次同一最小修复；冲突、停止和边界拒绝包只作为证据保留，没有带入临时产物。

## 验证与撤回

`py -3 -B sts2-ascend/brain/selfcheck.py` 输出 `SELFCHECK OK`。夹具覆盖默认 50 tick、关闭致死扩展回落到 Boss 40 tick，以及非致死 Boss 不越过既有窗口。若后续 3—10 局没有在 40—50 tick 间恢复可执行牌，或延长窗口增加无效等待，则将配置值设为 0，回到原 Boss 窗口并转查 payload/API。

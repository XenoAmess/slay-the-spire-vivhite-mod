# Local packaging/build by-products.  Source contracts and fixtures remain tracked.
*.egg-info/
build/
dist/

# Real capture runs can contain large media and remain process evidence, but
# must never be staged accidentally with the project source.
runs/*
!runs/README.md

# 宣传片版本配置

这里的版本清单描述从同一批已验收素材重新编排的目标，不是对已签署 MP4 的隐式转码。每个版本在生产时都必须复制当前项目配置快照、创建新的 `run_id`/attempt，并重新通过技术审计、白绮语义审计和人工审核。

```text
hero-60.json  60 秒主推剪辑
cut-30.json   30 秒短剪辑
cut-15.json   15 秒短剪辑
```

清单不包含媒体路径或凭据；实际 span、证据和媒体字节仍由该次 run 的 capture contract 绑定。

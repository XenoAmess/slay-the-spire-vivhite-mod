# 白绮替换原版战士：工作交接

日期：2026-08-28；最后更新：2026-08-29

范围：原版 `IRONCLAD` 的白绮完整视觉替换；独立角色 `VivhiteCharacter` 不在本阶段修改范围。

交接状态：V3 最终统一候选已完成最小字段总装、专项 validator、八动画精确 Vulkan、25 组
连续中断、真实 VFX 8 场景、merchant 十相位、严格 30 文件 Source/Godot/Spine 和完整
no-deploy PCK，并已于 2026-08-29 发布为正式五页 runtime、部署三件套及完成有范围的 Vulkan
真机验收。自然地图 marker、双客户端多人手势与休息点 `light_off` 仍保留为后续消费路径门禁。

## 1. 接手者先读什么

按以下顺序完整阅读，不要只看本交接摘要：

1. 根目录 `AGENTS.md`：付费生成、Alpha、八次上限、并发、构建、部署和直播边界；
2. `docs/白绮AI生成图Prompt工程手册.md`：EvoLink 请求与 Prompt 的长期事实源；
3. `docs/白绮战斗Sprite-Spine方案演进与生产方案.md`：骨架、动作、分层、VFX 和两条生产路线；
4. 本文：当前进度、证据位置和接下来可直接执行的顺序；
5. 各 `assets/vivhite-ironclad/evaluation/**/` 下的专项报告。

禁止事项仍然有效：白绮不能持剑、法杖或其他武器；不得套原战士骨架/网格/持剑姿势；需要
透明背景的新图只能走 EvoLink `gpt-image-2` 原生透明请求；不得程序抠图或修 Alpha；不得删除
任何付费原图、Prompt 或脱敏请求；不得擅自启动 Bilibili 直播。

## 2. 一句话现状

2026-08-29 部署前，仓库正式运行时确实还是 26 文件、单页 atlas、70% 骨架内部尺度的 legacy
版本；该历史结论现在已被明确的发布动作更新。正式 `Vivhite/Vivhite/skins/ironclad/` 当前为
严格五页、30 个逻辑文件的 `hybrid_v3_final`，修复后 Spine JSON SHA-256 为
`608DE3B142BB24A5D2BD402C24B6B1BAD4E643C0896D5931C854BAFE5353AAA1`，30 文件规范化清单
SHA-256 为 `03D8818137931A429810BBCB6F4700CFB8E356205B5F45621E9A4E9C2BE5E931`。

游戏 `mods/Vivhite` 已部署对应 DLL/json/PCK，Vulkan 日志确认 replacement 注册与 V3 skin
启用。角色选择、待机/低血、普攻、重击、施法、非致死受伤、死亡、普通商店、伪商人视觉路径
和三幕篝火均已真机覆盖；详细证据见
`docs/2026-08-29-白绮Hybrid-V3部署与真机验收.md`。整个替换项目仍不能无条件写成最终发布完成，
因为自然地图 marker、多人端到端和 `light_off` 尚有明确证据缺口。本轮没有操作 Bilibili 直播。

## 3. Git 与工作区状态

### 3.1 与本任务直接相关的增量提交

以下提交是接手时最有用的定位点；历史中间可能穿插自动游玩 checkpoint：

| 提交 | 内容 |
| --- | --- |
| `069670c3` | 正式 legacy 战斗人物内部尺度改为用户指定的 70%。 |
| `2ea0c52a` | V3 独立死亡连续性：16/16 精确 Vulkan。 |
| `0663bbbd` | 蓝蝶单一刚性 region 消费者。 |
| `45ef3dbe` | 远侧手臂两件制灰盒契约。 |
| `d3201ad3` | `0031` weighted 后发消费者。 |
| `8d410aa5` | 完整保留右小腿 `0096–0103` 八次付费尝试。 |
| `d66b936b` | legacy 26 文件与 V3 五页/30 文件双契约。 |
| `0ccda063` | V3 cast、hurt、neutral/merchant 分项候选与证据。 |
| `a3e9b4b8` | 头脸、双臂、双腿、躯干裙摆、总装 A/B 研究证据。 |
| `243d1a43` | 篝火、选人和多人手势的离线验收工具/证据。 |
| `5ec22e16` | 创建并验证 Hybrid V3 五页最终隔离候选。 |
| `41ccbab4` | 修复 cast 重启残眼并固化统一离线验收、真实 VFX 与 merchant 工具。 |

本交接文档和两份长期文档的状态修订另有收尾提交；以 `git log --oneline -- docs/` 的最新记录
为准。

### 3.2 不要清理共享脏状态

该工作区同时被 `sts2-ascend` 和直播维护链使用，普通 Git index 中可能存在与本美术任务无关的
暂存/修改，例如 Bilibili 脚本、`AGENTS.md`、自动游玩日志和临时截图。不要用 `git reset --hard`、
`git checkout --` 或批量删除来“清理”。提交本任务时应使用从当前 `refs/heads/master` 读取的私有
index，只精确加入本任务路径，再用 compare-and-swap 更新分支。

本轮已明确排除的无关路径包括但不限于：

- `.agents/skills/bilibili-live/SKILL.md`；
- 根 `AGENTS.md` 的其他并发改动；
- `sts2-ascend/**` 的运行、复盘和 Bilibili 改动；
- `.tmp/**` 与 `.codex/**`；
- 无明确归属的 `tools/art/render_ironclad_skin_preview.gd` 工作树改动。

## 4. 正式 runtime 与候选的边界

### 4.1 当前正式 runtime

`Vivhite/Vivhite/skins/ironclad/` 当前严格为 V3 的 30 个逻辑文件：

- combat：五张固定 atlas 页 + `.spatlas` / `.spjson` / skeleton-data `.tres`；
- merchant：复用同一五页 combat Spine 数据；
- rest site、character select：各自私有场景和骨架；
- UI：头像、轮廓、地图标记、普通/锁定选人图；
- multiplayer：四张用户明确豁免的历史手势。

部署前的 26 文件 legacy runtime 与游戏三件套已完整保存在
`.work/hybrid-v3-deploy/backup-20260829-005041-7733080/`，它现在是回退基线，不再是正式源目录
当前内容。研究输出仍不得直接覆盖这套正式 V3。

### 4.2 V3 五页契约

显式布局名：`v3-five-page`。combat 与 merchant 必须按同一顺序引用：

| 页 | 尺寸 | 内容 |
| --- | --- | --- |
| `vivhite_combat.png` | `3072×2304` | neutral、magic arc、sigil |
| `vivhite_combat_death.png` | `2048×1536` | 独立侧卧死亡 |
| `vivhite_combat_attack.png` | `2048×2304` | 普攻峰值 |
| `vivhite_combat_attack_heavy.png` | `2048×2304` | 重击峰值 |
| `vivhite_combat_cast.png` | `2048×2304` | 施法峰值 |

契约实现位置：

- `Vivhite/tools/ironclad-skin.contract.json`；
- `tools/art/publish_ironclad_skin.py`；
- `Vivhite/tools/Validate-IroncladSkin.ps1`；
- `Vivhite/tools/Validate-IroncladSpine.gd`；
- `Vivhite/VivhiteCode/Characters/IroncladReplacementAssets.cs`。

默认仍是 `legacy-single-page`。只有完整五页、完整 30 个私有运行时文件才会被接受；部分页、
错页名或 legacy/V3 混合均应 fail closed。已验证：Python 契约测试 4/4、C# build 0 warning /
0 error、legacy 与 V3 fixture、Godot/Spine 加载、最小 PCK。fixture 位于 ignored 的
`.work/multi-page-contract/`，源码测试已在仓库；这里的“最小 PCK”只是布局 fixture，不代表
`hybrid_v3_final` 的完整隔离 DLL/PCK 门禁。

## 5. Hybrid V3 主线的真实状态

| 部分 | 当前候选/素材 | 离线结论 | 2026-08-29 真机结论 |
| --- | --- | --- | --- |
| neutral | `hybrid_neutral_v3` | 19/19 精确时刻；总装 merchant 10/10 dirty seek 只显示 body。 | 战斗待机、低血循环、普通商店 >13 秒和伪商人视觉路径均稳定；人物仍明显大于原战士，这是已知 1.389× 选择而非加载错误。 |
| 普攻 | `0104` / `hybrid_attack_peak` | 14/14；窗口 `[.08,.20)`；mix `.10s`；魔法弧 `(210,30)`。 | 实际出牌与伤害成功，人物峰值和魔法弧出现并正常恢复，未观察到阻断性 recovery 跳变。 |
| 重击 | `0106` / `hybrid_action_set` | 14/14；窗口 `[.12,.32)`；mix 约 `.02s`；魔法弧 `(210,30)`。 | 实际重击与伤害成功，峰值/VFX/恢复正常，未观察到阻断性位移。 |
| 施法 | `0107` / `hybrid_cast_set` | 14/14；窗口 `[.25,.60)`；事件 `clear@0 → eyes@.25 → clear@1.222`；真实 VFX 8/8。 | 实际 `RAGE` 成功，施法姿势、法阵和 EyeFire 正常；无旧眼火残留。 |
| 受伤 | `hybrid_hurt_neutral` | 不增新图；neutral 网格完成收缩、后撤、回弹；统一连续中断门禁通过。 | 非致死伤害 HP 42→40 并正常回待机；本轮画面变化细微，只标功能通过，不夸大主观幅度。 |
| 死亡 | `0029` / `hybrid_death_v3` | 16/16；`1.05s` 原子切换；热动作进入 die 已通过；实体横跳由 61px 降为 16px。 | 40 帧覆盖倒下、侧卧和结算，单人物通过。曾误把侧卧人物的两条腿/衣料看成重叠，放大与单 region/单 SpineSprite 证据已纠正。 |

### 5.1 施法锚点不要退回旧值

初版 `(40,40)` 在 composite 中明显悬空；该失败是消费者坐标问题，不是 `0107` 生成图问题。
冻结值：

- cast attachment 可见的 `[.25,.60)`：`(194,-292)`；
- `.60` 切回 neutral 后至 `clear_vfx`：`(72,-282)`；
- `clear_vfx=1.222000026`。

证据：`assets/vivhite-ironclad/evaluation/v3-cast-0107-exact/`。

### 5.2 neutral 的尺寸事实

当前 70% 版本在 `A>=128` 下约为：

- idle：`203–211 × 348–353 px`；
- low health：`218–221 × 345–348 px`；
- relaxed：`203–211 × 348–351 px`。

原版战士对照高度约 `252 px`，因此白绮仍约高 `1.389×`。不要把用户选择 70% 理解成“已与
原战士同尺寸”。最终真机要在战斗、商店同时检查 Bounds、血条、意图和 VFX；若再调尺寸，
仍应保持场景 `.28`，只改骨架内部尺度，且重新走全部场景回归。

### 5.3 已创建的隔离最终目录

本轮已经创建：

- `tools/art/candidates/hybrid_v3_final/`；
- `Vivhite/tools/candidates/hybrid_v3_final/`；
- `.work/hybrid-v3-final/`（ignored 的运行证据与临时输出）。

`Vivhite/tools/candidates/hybrid_v3_final/` 的 authored 基线严格为五张 PNG、`.spjson`、
`.spatlas`、`.tres` 共 8 文件；`.import/.uid` 只是不入库的 Godot cache。总装以 cast-set 为
结构基线，只移植 neutral 三循环 slots、hurt bones 和 death 两个 bone 子树；不得再整包覆盖
任一分项候选。该隔离目录现在仍是可复跑源；正式 runtime 已于 2026-08-29 从它精确发布，不能
再把这里的部署前说明当成当前状态。

## 6. 拆件 + 骨骼并行研究线

拆件线已经证明“消费结构可以做”，没有证明“生产美术已经齐全”。统一 A/B 灰盒按两候选 ×
八动画 × 21 帧共 336/336 通过，但候选明确写有 `deployable=false` 和
`production_runtime_ready_slots=[]`。

| 组件 | 冻结结构 | 结果 | 生产状态 |
| --- | --- | --- | --- |
| 蓝蝶 | 单一刚性 region，父骨 `vivhite_head` | 16/16；商店随机 seek 通过。 | `0030` 可作真实输入，待全身总装。 |
| 后发 | 单张 weighted cap，49 顶点/72 三角 | 168/168；relaxed 首尾闭环。 | `0031` 可作真实输入，待全身总装。 |
| 头脸 | `0045` weighted 首选，rigid 回退 | 三候选共 120/120。 | research-only；真实 EyeFire 总场景待测。 |
| 远臂 | `far_upper_arm + far_forearm_hand` | 40/40 灰盒。 | 无可发布美术；不拆腕。 |
| 近臂 | 上臂袖 + 前臂手；掌心只作内部 deform | 9/9 灰盒。 | 无可发布美术；slash 独立。 |
| 远腿 | `far_thigh + far_lower_leg_with_boot` | 结构通过。 | `0078/0088/0063` 仅研究。 |
| 近腿 | `right_thigh + right_lower_leg_and_boot_union` | 两件制优于旧三件制。 | `0064–0071` 方向错；`0083/0100` 仅研究。 |
| 躯干裙摆 | torso core + 四裙片 + 远肩饰前/后 | 灰盒通过。 | `0054` 生产失败；本语义已 8/8。 |

正确总层序至少应包含：`后发 → 躯干 → 头脸 → 蓝蝶 → 前刘海`。旧 head-face 接触表使用过
相反的蝶饰/前发顺序，只能证明头部网格；整合必须采用蓝蝶专项纠正后的顺序。

### 6.1 完全缺失的 13 个生产附件

1. `torso_core`；
2. skirt front-left；
3. skirt front-right；
4. skirt back-left；
5. skirt back-right；
6. far shoulder ornament back；
7. far shoulder ornament front；
8. far upper-arm sleeve；
9. near upper-arm sleeve；
10. far forearm-hand；
11. near forearm-hand；
12. far lower-leg-with-boot；
13. near lower-leg-with-boot。

此外，头脸、前发和双大腿仍是研究件，不等于生产批准。

### 6.2 额度门禁

躯干语义 `0048–0055` 已经 8/8。`0055` 即使没有 PNG 也算一次已发生的付费尝试。未经用户
明确为该语义追加额度，不得用 `torso_core`、skirt 或 shoulder 的新名称规避第九次门禁。
其他达到 8/8 的旧腿/头微件也只保留作审计和研究；是否追加额度由用户统一评审决定。

所有 `0096–0103` 原始结果、逐字 Prompt 与脱敏参数已在仓库追加保存。失败图不可删除，也不可
程序拼接成生产素材。

## 7. 非战斗场景和 UI

### 7.1 篝火

`assets/vivhite-ironclad/evaluation/rest-site-acceptance/` 已固化 14 个文件。Windows Vulkan 共
73 帧、219 个 SourceOver 合成、11 张接触表通过；三个章节循环、翻转、light on/off 均通过。
白绮约 `362–364×480 px`，原战士约 `304×488 px`：高度约 98.4%，宽度约 119%。无需重生，
2026-08-29 后置复跑结果与固化报告除时间戳外逐字段相同。随后真机已覆盖第一幕 overgrowth、
第二幕 hive、第三幕 glory 循环与第一幕休息操作；三幕人物均正确且稳定跨过循环边界。第一幕
休息后没有观察到 `light_off` 的可辨变化。反编译显示基础 `RestSiteSynchronizer` 在清空选项前
触发回调，`ExtinguishFireIfAble` 因仍看到剩余选项而返回；当前将它记录为 consumer 顺序缺口，
不把离线 `light_off` 图片/动画门禁改写为失败。

### 7.2 角色选择

可复跑工具在 `tools/art/candidates/character_select_acceptance/`。本轮 ignored 证据在
`.work/character-select-regression-20260829-000559-cfd7a4ef/`：真实私有场景 5/5、hero-only 5/5、
15 个接触表 placement 与五张 UI × 三底 15/15 均通过。
保留警告：`icon.png` 和 `icon_outline.png` 的 Alpha 使用矩形触及上下边；实际 SourceOver 没有
可见矩形光晕。如果最终门禁要求四边严格全透明，需要轻微内缩后重验。游戏内
`NSpineAutoPlayer._Ready`、replacement 注册、原版 `IRONCLAD` 真实按钮与切换返回已真机通过。
顶栏头像也在多个场景正常显示；地图覆盖层能打开，但调试跳房导致当前位置无效，本轮没有形成
可验的自然地图 marker 证据。

### 7.3 多人手势

四张图是四个独立 `422×1200` RGBA 纹理，不是一张四格 spritesheet；运行时约
`383×1072`。用户已明确允许这四张从历史污染隔离区恢复，离线加载和三底 Vulkan 通过。
`point` 指尖是非阻断警告：相对原版向右约 85.9 consumer px，相对固定 pivot 向右约 56.1px。
2026-08-29 后置 wrapper 再确认 4/4 封闭例外源、runtime、尺寸、四角 Alpha 和哈希完全一致。
没有第二客户端，所以只能写“资源与离线渲染通过”，不能声称联机端到端通过。

### 7.4 商店与 VFX 的离线及真机边界

- merchant 使用正式 PackedScene 布局、`.28` 缩放与最终候选完成 10/10 deterministic dirty seek，
  均为 body-only 并通过三底 SourceOver。standalone 未绑定 `NMerchantCharacter`，因此只称“真实
  布局 + seek 合约代理”；正式游戏中的普通商店现已通过 `NMerchantCharacter`、库存开关和
  >13 秒循环。伪商人也已实际显示 combat rig 的 `relaxed_loop` 并稳定 >13 秒；调试事件后的 Agent
  API 状态仍滞留在旧 `REWARD`，故只称伪商人视觉路径通过，不称 API/库存端到端通过；
- 真实 `NIroncladVfx` 修复前为 7/8，`clear_vfx@0` A/B 后为 8/8，没有新增正式 C# patch；
- 25/25 连续 Spine 序列未复现 slot 残留，因此没有增加批量 t0 reset；
- acceptance-only `RuntimeVfxHarness.cs` 只在隔离 bridge 中编译，不进入正式 Mod DLL；
- `.work/combat-vfx-acceptance/runtime-actionset-a/b/c` 仍是无效尝试，不可作为通过证据；
- 完整 combat layout 专项不同于 exact/transition consumer，仍留作真机前的独立门禁。

## 8. 本机 ignored 证据

这些目录没有进入 Git，但在当前机器上仍可供接手者查看：

- `.work/combat-rig-compare-preview/hybrid-neutral-v3-exact-20260828-d/`；
- `.work/combat-rig-compare-preview/hybrid-neutral-v3-exact-20260828-d/contact-sheets/neutral-all-game-green-focus.png`；
- `.work/combat-rig-compare-preview/semantic-split-v3-ab/summary.json`；
- `.work/character-select-acceptance/`；
- `.work/multiplayer-ui-acceptance/`；
- `.work/multi-page-contract/v3-contract-fixture.pck`；
- `.work/hybrid-v3-final/cast-clear0-exact-20260828-154858/`；
- `.work/hybrid-v3-final-transition-probe-07/transition_summary.json`；
- `.work/hybrid-v3-final-runtime-vfx/20260828-234719-147ebec8/evidence/summary.json`；
- `.work/merchant-vulkan-20260828-152958-5489578/evidence-current/merchant_summary.json`；
- `.work/hybrid-v3-final/wrapper-full-final-20260829-001049/validation_summary.json`；
- `.work/rest-site-acceptance/hybrid-v3-final-regression-20260829-000417-276/report.json`；
- `.work/character-select-regression-20260829-000559-cfd7a4ef/`；
- `.work/hybrid-v3-final-multiplayer-regression-20260829-001/report.json`。
- `.work/hybrid-v3-final/pck-fixed-20260829-003420-4471698/pck_validation_summary.json`。
- `.work/hybrid-v3-deploy/backup-20260829-005041-7733080/`；
- `.work/hybrid-v3-deploy/runtime-20260829-v3/`；

它们是方便复核的本地证据，不是仓库事实源；需要跨机器长期保存的结论已经写入本文、两份活
文档或受跟踪的 `assets/**/evaluation/`。不要把 `.work` 整目录打进发布 PCK。

## 9. 推荐接手顺序

### 阶段 A：统一候选，不碰正式 runtime

已完成：cast-set 结构基线、五个语义页供体、neutral loop slots、hurt bones、death 两 bone 的
最小合并已输出到 `hybrid_v3_final`。总装器连续两次得到相同 8 文件哈希；专项 validator 通过
8 authored / 5 pages / 35 bones / 6 slots / 8 animations / 4 events 和 73 次 runtime 检查。
真实 consumer 复现并修复了 `cast→cast` EyeFire 残留：最终 cast 事件为
`clear_vfx@0 → cast_eyes_start@.25 → clear_vfx@1.222000026`。正式 skin 未修改。

### 阶段 B：统一离线 Vulkan

至少覆盖：

- 八动画实际时长的边界前/中/后帧；
- `[.08,.20)`、`[.12,.32)`、`[.25,.60)`、`die@1.05` 的原子前后帧；
- attack/heavy/cast/hurt/die 互相中断和返回 idle；
- character-only 与 composite 分层；
- slash、EyeFire、sigil 的出现、pose-specific 锚点和 `clear_vfx`；
- merchant `relaxed_loop` 的随机 seek 与两个循环边界；
- 单人物、无空帧、无触边、无双影、无武器、无旧战士残片。

当前结果：八动画 fresh exact 共 84/84；同一 AnimationState 的 25/25 序列、104/104 checkpoint、
50/50 target t0 / mix+ε 通过；真实 `NIroncladVfx` 8/8；merchant PackedScene + dirty seek 10/10。
`NMerchantCharacter` 在 standalone 中未绑定，故该段只标记真实场景布局 + consumer 合约代理。

### 阶段 C：不部署的完整构建

完整隔离 PCK 必须使用正式 wrapper；它只接受前一阶段的 `success=true` 摘要，并要求新的
`.work` 输出目录：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File `
  .\tools\art\candidates\hybrid_v3_final\Invoke-HybridV3FinalPckValidation.ps1 `
  -ValidationOutputDir .work\hybrid-v3-final\wrapper-full-final-20260829-001049 `
  -OutputDir <新的 .work 子目录>
```

只有 `hybrid_v3_final` 通过前两阶段，才在 `.work/hybrid-v3-final/**` 的**完整隔离项目副本**中
测试五页 PCK。旧写法只加 `CopyModOnBuild=false` 是错误的：它只禁 DLL/json 的 `CopyMod`，
不会阻止 `ExportPCK` 使用默认 `ModPckPath` 覆盖游戏目录。安全命令必须同时显式设置：

- `/p:IroncladSkinRuntimeLayout=v3-five-page`；
- `/p:CopyModOnBuild=false` 与 `/p:RitsuLibAutoCopy=false`；
- `ModPckPath`、`ModOutputDir`、`RitsuLibDeployDir` 全部指向 `.work`；
- `ProjectDir` 使用隔离副本，避免 Source validator、Godot import 与 manifest 同步写正式项目。

发布工具与 PCK validator 都必须读取隔离 30 文件 runtime；以该 PCK wrapper 和
`python tools/art/publish_ironclad_skin.py --help` 为准，不再复制旧的不安全示例。最终复跑目录为
`.work/hybrid-v3-final/pck-fixed-20260829-003420-4471698/`：输入/发布均 30 文件，build 为 0 warning /
0 error，PCK 为 101 entries、14,598,276 bytes，SHA-256 为
`F674613294E7BA69FF823A83AB1E465A8F730D6E5DB59791F343EE68608A84D0`；正式 runtime、游戏
`mods/Vivhite` 与 `mods/STS2-RitsuLib` 三棵哈希树前后不变，且 `deployed_to_game=false`。

首轮同内容 PCK 虽通过，但 `Start-Process -Wait` 被 Roslyn `VBCSCompiler` server 拖延约十分钟。
wrapper 增加 `/p:UseSharedCompilation=false` 后在全新目录用 205.833 秒完成，没有遗留 compiler
server。不能把“build 日志已结束”直接当作 wrapper 已结束，也不要通过泛杀 dotnet 解决。

### 阶段 D：真实游戏

离线全部通过后才进行：

1. 比对并备份当前 Vivhite dll/json/pck 三件套及 SHA-256；
2. 显式发布五页布局并完整构建；
3. 使用 Vulkan 启动，不启动自动点击；
4. 90 秒内主菜单失败、崩溃或人物不可见，立即恢复旧三件套；
5. 依次验收选人、战斗八动画/VFX、商店、伪商人、三章篝火、UI、多人离线尺寸；
6. 检查新会话日志中所有 `res://Vivhite/skins/ironclad/` 相关错误；
7. 用户真机认可后才把 V3 视为可发布版本。

2026-08-29 执行结果：上述备份、显式五页发布、完整构建、Vulkan 启动、日志检查和有范围真机
验收均已执行。正式 30 文件清单 SHA-256 为
`03D8818137931A429810BBCB6F4700CFB8E356205B5F45621E9A4E9C2BE5E931`；部署 PCK 仍为前述
no-deploy 验收的 101 entries / 14,598,276 bytes / SHA-256
`F674613294E7BA69FF823A83AB1E465A8F730D6E5DB59791F343EE68608A84D0`。详细三件套哈希、真机矩阵、
截图与日志边界见 `docs/2026-08-29-白绮Hybrid-V3部署与真机验收.md`。

尚未完成的自然消费门禁只有：有效地图位置上的 marker、双客户端多人手势，以及基础 consumer
顺序明确后对 `light_off` 的复验。不要把调试地图无 marker 或休息回调顺序问题倒推成图片需要
重生成。

严禁在上述流程中调用 Bilibili 开播。用户以后明确要求“启动整套游戏 + brain + TTS”时，才按
`sts2-ascend/scripts/Start-Agent.ps1` 统一入口执行；它与美术真机验收是两件事。

## 10. 最容易踩的坑

1. **继续把部署前状态当成当前状态。** 2026-08-29 起正式 runtime 和游戏三件套已经是 V3；
   旧 legacy 只存在于精确备份。剩余缺口是明确列出的自然 marker、多人端到端和 `light_off`，
   不是 combat layout、总装或完整 PCK。
2. **把 70% 当成与原战士等高。** 当前仍约 1.389×；必须看真实场景锚点。
3. **退回 EyeFire `(40,40)`。** 已有悬空证据，使用双锚点。
4. **让商店随机 seek 出现动作残片。** `relaxed_loop` 两端和任意相位都必须重置所有非 body slot。
5. **把拆件灰盒当生产图。** 13 个附件完全缺失，头脸/前发/双大腿也只是研究件。
6. **用新名字重置 8 次额度。** 躯干组已 8/8；语义相同就仍是同一额度。
7. **误判画布。** 每张图先核对 atlas/Spine/源码消费者；多人四图是四个独立纹理。
8. **凭透明直显判断光晕。** 必须真实 SourceOver；同时严禁用程序修改 Alpha。
9. **污染正式 runtime。** 研究输出只放候选目录；全部门禁通过后才发布。
10. **误提交共享工作区。** 精确路径、私有 index、CAS；保留所有无关用户/自动化改动。

## 11. 收尾时已经完成的检查

- V3 五页/30 文件已精确发布；旧 26 文件 runtime、旧三件套、依赖与日志已有完整回退包；
- Source、Godot/Spine、PCK 与 Vulkan 初始化日志已通过，未出现私有 V3 资源错误；
- 角色选择、主要战斗动作、低血、死亡、普通/伪商人视觉及三幕篝火已有真机证据；
- 死亡缩略图曾被错误解释为“双人物”，用户纠正后按放大轮廓、单 atlas region、单 SpineSprite
  和互斥 slot 复核为单人物，不修改素材、不增加无必要门禁；
- 3 号隔离档没有删除，1 号原档已恢复；完整 `sts2-ascend` 栈已用统一入口和 `-SkipDeploy`
  复用 Vulkan 游戏恢复，API 为 ready；
- 本轮没有付费生成、Prompt/Alpha 变化或 Bilibili 直播操作。

## 12. 完成定义

本任务只有在以下条件同时满足时才可写“完成”：

- V3 五页/30 文件最终候选生成并通过严格契约；
- 八动画、所有 mix/事件/VFX、中断恢复、merchant seek 离线 Vulkan 通过；
- 角色选择、篝火、UI 和多人资源回归通过；
- 不部署完整 build/PCK 通过；
- 备份、部署、Vulkan 真机、日志和用户视觉验收通过；
- 原版战士玩法、能量球、卡池、音频不变，独立白绮角色仍可选择；
- 用户之后明确要求时，才安装最终 Mod 并通过统一入口恢复游戏 + brain + TTS 训练。

当前准确状态是：**V3 隔离总装、精确/连续 Vulkan、真实 VFX、merchant、严格 30 文件、非战斗
资源回归、完整 no-deploy PCK、正式发布和本轮有范围真机验收均已完成；正式 runtime 与游戏三件套
已经是 V3。整个替换项目仍保留自然地图 marker、双客户端多人手势和 `light_off` consumer 顺序
复验三项缺口，因此不冒充无条件最终发布完成。**

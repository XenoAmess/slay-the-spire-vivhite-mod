# Vivhite 0.2.1 Workshop 更新回执

## 发布结论

2026-09-01 20:39（北京时间），仓库统一发布入口成功更新既有 Steam Workshop 条目
`3793741497`，没有创建重复条目：

- 条目：<https://steamcommunity.com/sharedfiles/filedetails/?id=3793741497>
- 版本：`0.2.1`
- 可见性：`public`
- 发布回执：`status=published`、`upload_complete=true`、`created=false`
- RitsuLib 依赖：`3747602295`，`dependency_complete=true`
- Workshop 协议：`workshop_agreement_required=false`
- 完成时间：`2026-09-01T12:39:22.9218910Z`

实际执行命令为：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\workshop\Publish-VivhiteWorkshop.ps1 -PublishedFileId 3793741497 -Visibility public
```

没有使用 `-SkipBuild`、`-SkipPreview` 或 `-PrepareOnly`。发布入口重新执行了隔离
Release 构建、PCK 内容门禁、预览图确定性生成、BBCode/版本/依赖一致性检查，并把从
双语 Changelog 派生的 `change-note-v0.2.1.txt` 交给 Steam uploader。

## 发布包与物料证据

发布前 PCK 门禁通过：323 entries、Godot 4.5.1、pack format 3；运行时美术
`92/92`、卡牌 `61/61`、Power `19/19`、皇冠 `2/2`、能量 UI `7/7`、VFX
`3/3`、中英文 localization 各 `314/314`，NOPE/generic fallback 为 0。

| 文件 | bytes | SHA-256 |
| --- | ---: | --- |
| `Vivhite.dll` | 226,816 | `FC5775F9DD90F3A46D7EA3953EE4BB9B2F56F7E3FEEC078CB1D912D2D9232E57` |
| `Vivhite.json` | 434 | `3F24A617FA15B78E390430677E0698840092A0F4DCF2D85017B68CB4CD318215` |
| `Vivhite.pck` | 95,863,716 | `4D31FC518216BDD5E276D7577C31840CF4A57D471CCAE39D0774C15B243F7619` |

三件套合计 `96,090,966` bytes，与 Steam Web API 的远端 `file_size` 一致。

- `workshop/preview.jpg`：1024×1024，170,282 bytes，SHA-256
  `17D3DCF4560FC10D13634784CA7CAE2A8718EBAF6EDC333DAE9000E80CA00F86`
- 上传 change note：1,268 UTF-8 bytes，SHA-256
  `602984459EFF86A7F5B38D8287B6ADC6A9FD23F5BD3E11E072EF3037E3EF6AA8`
- 发布时 preflight HEAD：`9d9e5f2bf944134f1cb13492c079657147ec55b9`

Steam 云日志记录预览图 UGC 上传成功；Web API 随后返回 `result=1`、`visibility=0`、
`banned=0`，公开描述已包含中英文 `0.2.1` Changelog、`public-beta` 验证范围、
Tungsten Rod/Buffer 生命支付修复、Event Loop 临时复制牌修复，以及 Vulkan / OpenGL3 /
D3D12 的兼容性边界。

## 发布前训练稳定性证据

发布不是在 `Stack ready` 或主菜单心跳后立即执行。下播训练 session
`63678f513c0944deadc771095bda634b` 已恢复原生 run
`4MALNYXSZ5CV`（白绮第 7 局），并在发布前满足以下证据：

- `/state` 从 `MAIN_MENU/run_unknown` 恢复为有效 run；
- 驾驶舱持续 `connected`；
- 多次 `play_card`、`end_turn`、`select_deck_card`、`choose_map_node` 等动作收到
  `outcome.status=applied`；
- 对局从 F17 连续推进到 F29，随后在 F31 Boss 长战斗中继续推进回合与动作；
- D: 可用空间稳定在约 96.065 GiB，Steam 云日志没有新的 `IOFailure` 或 build-list
  conflict；
- Bilibili Livehime 始终为 `NotRunning`，没有开播、自动复播、人工输入或 UAC。

## 后续注意

以后每次 Workshop 更新仍必须使用同一入口完整重建，不得用 `-SkipPreview` 绕过预览
物料门禁；必须同步维护 `Vivhite.json`、`workshop-item.json`、双语
`description.bbcode` Changelog、`preview.jpg` 与 `preview-history`，并以结构化发布回执及
远端只读元数据确认作为“已发布”的判断依据。

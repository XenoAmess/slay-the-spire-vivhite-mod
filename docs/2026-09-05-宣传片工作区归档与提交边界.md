# 宣传片工作区归档与提交边界

## 原因与统计

用户先要求分析“60K 新增代码”是否提交或忽略，随后明确同意按分类处理。
整理前，Git 未跟踪文件共 315 个，其中 80 个文本文件共 164,274 个物理行
（含注释、空行，不是有效代码行数）：

| 内容 | 文件数 | 文本行数 | 处理 |
| --- | ---: | ---: | --- |
| 误落在根目录的 FFmpeg signalstats 日志 | 3 | 130,413 | 保全后移入忽略的 run 归档 |
| 临时 PNG/JPG 截图、抽帧 | 235 | 不适用 | 同上，约 147 MiB |
| Python / PowerShell（含测试） | 53 | 27,270 | 保留有效源码；其中 5 个过时脚本归档 |
| JSON / Markdown | 24 | 6,591 | 保留创作输入、契约和文档 |

已有跟踪文件的差异另为 +374 / -194 行，其中 Mod 的 C# 差异仅 +62 行。
不能将界面新增计数直接解释成同等规模的 Mod 功能代码。

## 归档与恢复

归档目录：`tools/promo/runs/run-20260903T0012-director-v2-a1/source-cleanup-20260905-a1/`。
共迁移 **243 个文件、158,118,310 字节**，移动前后逐一核对 SHA-256 一致；
没有删除原始内容，也没有覆盖已有录制、成片、证据或失败尝试。

该目录的 `relocation-manifest.json` 记录全部原路径、新路径、类别、字节数和 SHA-256，
自身 SHA-256 为 `ecba7993e3112c8beb15ddd76e15a0bca5e2a897caad76b2e55795f37cc297f8`。
内容按 `files/<原仓库相对路径>` 保存。需要恢复时按清单逐项恢复，先确认目标不存在，
再校验哈希；过时脚本是历史源码快照，不应从新的归档位置直接运行。

归档的 5 个脚本如下，源码搜索确认保留脚本未引用它们：

- `tools/promo/archive_t07_a07.py`
- `tools/promo/archive_t16_a11.py`
- `tools/promo/archive_t16_a18.py`
- `tools/promo/archive_t16_a19.py`
- `tools/promo/archive_t18_a04.py`

上述 5 个脚本合计 2,664 行。其余 9 个归档入口/配方共 5,476 行，仍包含参数化入口
或当前采用素材的制作依据，保留在源码区。特别是 `archive_t16_luminous_attempts.py`
还包含当前采用的 T16/a22，不能仅凭旧 attempt 名称判为废弃文件。

## 版本化边界

- 提交宣传片模块、测试、schema、拍摄辅助、导演分镜、旁白、剪辑配方及成片装配脚本。
  `narration-script.json` 即使由生成器确定性物化，也是可审阅的制作输入；不忽略全部 JSON。
- 实录、抽帧、探针、动作回执、实际 EDL、渲染结果、日志仍由 `tools/promo/.gitignore`
  的 `runs/*` 规则隔离。根 `.gitignore` 仅补充旧根目录临时图片和误拼接扫描日志的窄规则。
- 不忽略整个 `tools/promo/`、`v2/`、`archive_*.py` 或所有 Python/Markdown 文件。
- `sts2-ascend/knowledge/profiles/vivhite/review_queue.json`、`knowledge/review_queue.json`
  和 `knowledge/stats.json` 是原有训练状态，本次不修改、暂存或提交，也不改变其追踪规则。
- 忽略仅表示不进入 Git；归档仍只在本机。恢复实录成片需要另行保全对应 run 的媒体，
  仅克隆源码仓库不包含录像，固定批次的制作脚本也不是不依赖素材的一键重拍入口。

## 验证与范围

- 宣传片离线 unittest：189 项通过，0 失败、0 跳过；新增 39 个 Python 文件内存编译通过。
- 新增 14 个 PowerShell 脚本及 `configure_obs.ps1`：15 个文件语法解析通过。
- C# 强制编译：0 警告、0 错误。使用 `--no-restore --no-incremental`，并关闭
  `RunPckExport`、`CopyModOnBuild`、`RitsuLibAutoCopy`，设置 `STS2_SKIP_PCK_EXPORT=1`；
  没有导出 PCK、部署到游戏、启动游戏/OBS/Brain、生成旁白或再次渲染。
- 本次是源码保全与 Git 整理，不代表新的真机画面验收、人工成片签字或 Workshop 发布。
- 非阻塞存量问题：`PromoCaptureSurface.cs` 的 `ProcessFrame` 匿名订阅在 manager 释放后
  未解绑，捕获模式反复重建场景可能积累空回调；本次未扩展修改运行逻辑。

# 人工接管契约与实现对齐

## 结果

工作树中的 `AGENTS.md`、`docs/2026-08-22-sts2-ascend自动游玩智能体.md` 和
`docs/2026-08-30-sts2-ascend-Brain人工接管快捷键.md` 曾被简化为只描述
`human_assisted` 标记；当前 Brain 实际实现还包含持久化学习回滚、F10 同局禁学、重启
fail-closed、审计轨迹保留以及统计/复盘/轮换隔离。本次已将这三份文件恢复到当前 HEAD
中的完整契约，避免文档弱于实现。

## 判断依据

- `Knowledge` 使用每个 Profile 的 `.active_run_learning.json` 保存局前基线和排除标记，
  构造及保存路径都会在排除局上恢复基线。
- Agent 在人工边界触及时阻断动作并排除该局；终局不调用正常 `finalize_run`，不推进统计、
  LLM 复盘或轮换配额。
- `test_manual_control`、`test_agent_profile_rotation_integration` 和
  `test_profile_floor_stats` 定向测试共 34 项全部通过。

## 运行时状态边界

`sts2-ascend/knowledge/profiles/vivhite/review_queue.json` 是 Brain 管理的运行时队列，
本次没有手工编辑、回滚、暂存或提交。它保持有效 JSON 和当前 retry/salvage 状态，后续
仍由 Brain 的统一生命周期负责更新。

# 白绮整身单网格骨链候选

## 目的与隔离边界

此候选用于与“拆件 + 骨骼”方案并排比较。它继续使用一张完整白绮身体图，但不再把所有变形控制骨直接挂在同一个根骨下，而是建立真实父子骨链。候选不部署、不修改当前运行时皮肤，也不操作游戏、brain 或直播。

- 构建器：`tools/art/candidates/whole_mesh/build_whole_mesh_candidate.gd`
- 无窗口 Spine 校验器：`tools/art/candidates/whole_mesh/validate_whole_mesh_candidate.gd`
- 自包含候选四件套：`Vivhite/tools/candidates/whole_mesh/`
- Godot 生成的 `.uid`、`.import` 只是被 Git 忽略的本地导入缓存，不属于候选包。

候选目录位于 Mod 工程已有的 `tools/**` PCK 排除范围内，因此普通导出不会把它误带进发布包。其 `.tres`、`.spatlas` 内部路径全部指向 `res://tools/candidates/whole_mesh/`，不引用或覆盖 `res://Vivhite/skins/ironclad/spine/combat/`。

## 素材与消费契约核对

输入的 `vivhite-combat-body-master-v1.png` 是一张完整 RGBA 人物母图，不是 spritesheet 或 packed atlas。构建产物则是一个三 region atlas：整身网格、独立魔法弧、独立魔法阵。实际战斗场景继续要求一个 `SpineSprite`，缩放为 `0.28`；源码与场景还要求 `slash_mesh`、`eye_attach_slot` 以及四个 VFX 事件。因此候选保留：

- 八个动画：`idle_loop`、`low_health_loop`、`relaxed_loop`、`attack`、`attack_heavy`、`cast`、`hurt`、`die`；
- 事件：`attack_slash_start`、`heavy_slash_start`、`cast_eyes_start`、`clear_vfx`；
- 插槽：`slash_mesh`、`eye_attach_slot`；
- 70% 骨架内部人物尺寸，即 `868 × 1302`，场景侧 `0.28` 不变；
- 单一 `vivhite_combat_body` 加权网格，345 个顶点、616 个三角形，不做身体 attachment 切换。

## 新骨链

候选共 33 根骨，关键父子关系为：

- `pelvis -> torso_lower -> torso_upper -> neck -> head`；
- `torso_upper -> shoulder -> upper_arm -> forearm -> hand`，左右各一条；
- `pelvis -> hip -> thigh -> shin -> foot`，左右各一条；
- 头发、蓝蝶跟随头部，裙摆跟随骨盆；
- `slash_mesh` 的魔法弧锚点跟随右手，眼部 VFX 锚点跟随头部。

网格每个顶点最多受四根邻近骨影响。与旧版全部平铺到 `vivhite_rig` 的结构相比，旋转肩膀会自然携带上臂、前臂、手和魔法弧，旋转躯干会携带头部和双臂，弯曲髋部会携带整条腿。

## 动作幅度

按战斗场景固定缩放 `0.28` 计算，构建门禁与 Spine 实际加载校验得到：

| 动作 | root 位移范围 |
| --- | ---: |
| 普通攻击 X | 27.44 px |
| 重击 X | 42.532 px |
| 受伤 X | 30.8 px |
| 施法 Y | 17.36 px |
| 待机 Y | 5.32 px |

普通攻击增加预备后撤、向前发力、回弹和回正；重击使用更大的重心与手臂链摆幅。施法让躯干上提、双臂经肩—肘—腕链明显展开。死亡不再做单段 `-76°` 翻牌，而是用八个 root 位置关键帧和多组七至八段关节关键帧实现“预备失衡 → 屈膝弯腰 → 侧倒 → 轻微过冲余震 → 静止”；`clear_vfx` 在 `t=0` 发出。

## 构建与校验

```powershell
$godot = 'C:\Users\xenoa\AppData\Local\Temp\opencode\godot\Godot_v4.5.1-stable_mono_win64\Godot_v4.5.1-stable_mono_win64_console.exe'

& $godot --headless --path tools/art `
  --script res://candidates/whole_mesh/build_whole_mesh_candidate.gd `
  -- build-combat

$env:DOTNET_ROOT = 'C:\Users\xenoa\AppData\Local\Microsoft\dotnet'
$env:DOTNET_ROOT_X64 = $env:DOTNET_ROOT
$env:PATH = "$env:DOTNET_ROOT;$env:PATH"

& $godot --headless --path Vivhite --import
& $godot --headless --path Vivhite `
  --script 'D:\workspace\slay-the-spire-vivhite-mod\tools\art\candidates\whole_mesh\validate_whole_mesh_candidate.gd'
```

离线结果：Spine 版本 `4.2.43`，8/8 动画、4/4 事件、4 个 slot、33 根骨均由真实 GDExtension 成功加载；八个动画分别在 0%、25%、50%、75%、100% 时间点完成状态应用。整个过程没有部署、停止或重启游戏。

## 已知限制

单网格方案仍有结构上限，不能仅靠增加骨骼完全消除：

- 手臂交叉身体时不能改变独立部件的前后遮挡顺序；
- 大角度肩、肘、膝弯曲会把原画像橡皮布一样拉伸，衣纹和轮廓可能剪切；
- 死亡只能把站立原画连续压弯成倒地轮廓，不能得到真正重新绘制的侧躺身体与独立散开的头发、裙摆；
- 母图自带的整身柔光会随网格一起弯曲。单网格不会产生拆件接缝，但强动作时柔光形状也会变形。

因此此候选适合用于确认“只加正确骨链”能提升到什么程度，不能替代后续 Vulkan 接触表与拆件候选的并排人工验收。

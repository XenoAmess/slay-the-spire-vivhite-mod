# Bilibili 开播记录

## 结果

通过 `sts2-ascend/scripts/Start-BilibiliLive.ps1` 执行开播。脚本确认已有 sts2-ascend 会话并完成 Livehime 开播触发；随后读取 Livehime 状态为 `Streaming`，游戏进程也正常存在。

## 注意事项

脚本最后尝试将游戏窗口切到前台时，当前 Codex 自动化会话收到 Windows 错误：`Could not make window ... the foreground window. Run from an interactive desktop session.` 这只影响前台焦点，不影响已经确认的直播状态；在真实交互式桌面中运行同一入口可完成置顶/前台步骤。

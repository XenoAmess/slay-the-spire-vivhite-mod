# 2026-08-22 ox-alpha 复盘模型能力评估（基于运行记录）

## 数据来源

- 复盘提交：2322b0a（第17局）、c494149（第18局）、d93fbd6（第26局）
- 复盘报告：`knowledge/meta_review.md`（137 行，中英文混排）
- 对局记录：30+ 局 runs/*.json；自检回滚备份目录（code_backups）为空

## 结论先行

**能做这个活，但建议保持"异步 + 安全网 + 双轨兜底"的架构。** 分析质量超出预期，
工程纪律中上，产出稳定性受免费模型可用性波动影响。

## 能力评估（按维度）

### 归因分析：强
实录（第18局复盘）：精确定位死因到"第二幕 Unknown 节点 THE_OBSCURA 单场 -59 血"，
并发现 `path_danger_priors.Unknown=10` 与实测单点杀伤 60+ 的**系统性低估**；
识别出"卡组膨胀 24 牌导致选牌硬阈值形同虚设"、"战斗末段意图 44 仍不补防"等行为级缺陷。
不是泛泛而谈，是拿着 run 日志逐项对数据的归因。

### 代码修改：中上
- 会做**结构性**改动而非只调参：`eval_reward_card` 重写为攻击占比自适应缩放、
  新增防御稀缺补偿、牌组软上限、`_score_play` 生存权重分档、地图幕间缩放。
- 改完主动跑 selfcheck；三次产生变更的复盘全部通过自检（零回滚）。
- 会**自我纠偏**：发现过自己上次"想把 card_pick_threshold 改 3.0 实际写成 2.0"的配置漂移。
- 会改进测试基建：给 selfcheck 增加了它自己提出的回归用例。

### 工程纪律：中上
- 有明确的边界意识（只动允许的文件、迁移数据结构时同步 knowledge.py）。
- 一次配置意图/执行不一致（3.0 写成 2.0），说明数值型修改需复核——这也正是自检+参数钳制存在的意义。

### 稳定性：中（免费模型的现实）
- 一次会话 4 分钟后 exit=1 中断；一次 25 分钟超时；可用性随 OpenRouter 免费额度波动。
- 单次复盘耗时长（10~40 分钟）——同步阻塞模式下不可接受，异步队列下无碍。

### 效果转化：待观察
0 胜记录主要受制于底层启发式引擎的早期 bug（弃牌 bug 22:47 才修）与样本量；
复盘给出的改进方向（防御权重、路径危险系数、卡组收敛）与人工判断一致。
其威胁感知评分改进曾被我方回滚事故误删（d93fbd6 留档可 cherry-pick）。

## 建议

1. 维持优先链 + 异步队列 + git 安全网现状。
2. 复盘的参数类变更已可放心；代码类变更继续依赖 selfcheck +  runner 重试兜底。
3. 若日后转付费/本地模型，评估指标看"复盘变更后 10 局的平均楼层"即可。

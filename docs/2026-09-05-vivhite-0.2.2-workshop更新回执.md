# Vivhite 0.2.2 Workshop 更新回执

## 发布结论

2026-09-05 13:29（北京时间），仓库统一发布入口成功更新既有 Steam Workshop 条目
`3793741497`，没有创建重复条目：

- 条目：<https://steamcommunity.com/sharedfiles/filedetails/?id=3793741497>
- 版本：`0.2.2`
- 可见性：`public`
- 发布回执：`status=published`、`upload_complete=true`、`created=false`
- RitsuLib 依赖：`3747602295`，`dependency_complete=true`
- Workshop 协议：`workshop_agreement_required=false`
- 完成时间：`2026-09-05T05:29:25.9747624Z`

实际执行命令为：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\workshop\Publish-VivhiteWorkshop.ps1 -PublishedFileId 3793741497 -Visibility public
```

没有使用 `-SkipBuild`、`-SkipPreview` 或 `-PrepareOnly`。发布前先在游戏和训练栈均已停止的
状态下关闭 Steam 客户端，把其离线意图从 `WantsOfflineMode=1` 改回 `0`，再启动客户端；
Steam 连接日志确认 `Connected`、`Logging On` 和登录响应 `OK` 后才运行发布入口。没有读取或
记录密码、Steam Guard、会话令牌等凭据。

## 本次 Changelog

本次上传的 change note 从 `workshop/description.bbcode` 的 0.2.2 中英文 Changelog 派生，
两种语言各包含同样四项事实：

1. “色彩守恒”升级后的謦欬生命消耗从 4 降至 2；
2. “无限画布”升级后的能量费用从 3 降至 2，并保留升级版汲取成长效果；
3. 新增全部 61 张卡牌的升级验收，以及钨合金棍、Buffer 生命支付和“事件循环”临时复制回归；
4. 当前发布构建覆盖 Steam `public-beta`、游戏 v0.111.0 和 Vulkan，OpenGL3 / D3D12 尚未完成
   Mod 专项验收。

上传 change note 为 1,374 UTF-8 bytes，SHA-256
`4DEE4CF72ADC984BC90C384F93039800A69E3271D40C245632582B2303A04D47`。

## 发布包与门禁证据

发布入口重新执行了隔离 Release 构建与挂载 PCK 验收：324 entries；运行时美术 `92/92`、
卡牌 `61/61`、Power `19/19`、皇冠 `2/2`、能量 UI `7/7`、VFX `3/3`，中英文
localization 各 `314/314`。构建为 0 warning、0 error。

| 文件 | bytes | SHA-256 |
| --- | ---: | --- |
| `Vivhite.dll` | 230,400 | `BC11E1E3DBDCBCFFE94AEF821EA8CEC6E352227940F7826A1D05C5B30CD921C9` |
| `Vivhite.json` | 434 | `CEA1F41B345333CD7E554FFE50BFD7EB04CD86A91068B22434A9B773C120BFD6` |
| `Vivhite.pck` | 95,863,856 | `ED2C88BE4E0092D5BED57869DEDB74DE1FA1D6C7147EAD32CDDD43E8F4BAA877` |

三件套合计 `96,094,690` bytes，与发布后的 Steam Web API `file_size` 完全一致。

- `workshop/preview.jpg`：1024×1024，170,357 bytes，SHA-256
  `DC9A57B681C91F610AE4068BCCC18CC54B12C53552BEFACF69C5071BE34AA161`
- 发布时 preflight HEAD：`7cd73c1f5469d06cda312725ed23d9157cde3210`
- RitsuLib manifest 依赖版本：`0.5.14`

## 远端只读复核

发布完成后，通过 Steam Web API 对条目重新查询，并直接下载 API 返回的远端预览 URL 在内存中
计算哈希：

- `result=1`、`publishedfileid=3793741497`、`visibility=0`、`banned=0`；
- 标题为“白绮 Vivhite | Custom Character”，远端更新时间为
  `2026-09-05T05:29:25Z`；
- 远端描述明确显示当前版本 `0.2.2`，且包含本次四条中英文 Changelog；
- 远端 `file_size=96094690`，与三件套合计一致；
- 远端预览为 170,357 bytes，SHA-256 为
  `DC9A57B681C91F610AE4068BCCC18CC54B12C53552BEFACF69C5071BE34AA161`，与仓库当前预览一致；
- 标签仍为 `english`、`Simplified Chinese`。

因此本次“已发布”同时满足结构化上传回执、依赖回执、远端元数据、远端内容大小和远端预览哈希
五层证据，不以本地 preflight 或进程退出码代替远端成功。

## 后续动作

本次发布完成后再将同一 0.2.2 源码完整构建并部署到本机游戏目录，然后只通过统一
`Start-Agent.ps1` 恢复游戏与 Brain 全栈。训练完成与 LLM 复盘成功另以新对局、动作、终局、
复盘提交和远端 Git 提交证据判断，不属于本发布回执的结论。

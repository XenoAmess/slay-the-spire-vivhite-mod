# sts2-ascend 全栈停止记录

## 结果

按“停止整套”语义执行 `sts2-ascend/scripts/Stop-Agent.ps1`。统一入口完成 session 哨兵协作退出、目标进程清理和游戏关闭，并报告 `scoped processes=0, game=stopped`。

## 停止后核验

- Bilibili Livehime：`NotRunning`
- `SlayTheSpire2.exe`：无残留进程
- Python brain/runner 组件：无残留进程

本次使用统一全栈停止入口，没有按端口或进程名进行泛化清理。

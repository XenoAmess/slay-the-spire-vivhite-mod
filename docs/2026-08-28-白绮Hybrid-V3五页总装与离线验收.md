# 白绮 Hybrid V3 五页总装与离线验收

日期：2026-08-28；最后更新：2026-08-29

## 当前结果

已创建隔离候选 `hybrid_v3_final`，没有修改正式
`Vivhite/Vivhite/skins/ironclad/**`，也没有向游戏 `mods` 部署。当前已通过：

- 总装器连续双跑确定性；
- 8 authored / 5 atlas pages / 35 bones / 6 slots / 8 animations / 4 events 专项门禁；
- 73 次 Spine runtime 语义检查；
- 八动画共 84 个 Windows + Vulkan 精确时刻；
- 25/25 连续中断、104/104 checkpoint 与 50/50 target t0 / mix+ε；
- 真实 `NIroncladVfx` 8/8 与 merchant 十相位；
- 严格 30 文件临时发布与 Source/Godot/Spine 四套资源门禁；
- 完整 no-deploy DLL/PCK：0 warning / 0 error、101 entries，三棵保护树前后不变。

正式 runtime 仍为 legacy，游戏 mods 未写入。本文件不能被解读为正式部署或用户真机通过。

## 总装边界

最终 JSON 以 `hybrid_cast_set` 为结构基线，只允许“四类 donor/结构差量 + 一项有真实 consumer
证据的事件修复”：

1. `skeleton.hash` 改为 `vivhite-hybrid-v3-final-v1`；
2. 三个 neutral loop 的完整 `.slots` 取自 `hybrid_neutral_v3`；
3. `animations.hurt.bones` 取自 `hybrid_hurt_neutral`；
4. `animations.die.bones` 中仅 `vivhite_rig` 与 `vivhite_death_pose` 取自
   `hybrid_death_v3`；
5. `animations.cast.events` 在 donor 序列前增加 `clear_vfx@0`。

没有整段覆盖 hurt 或 die。这样既保留专项骨动画，也保住 cast-set 中 action slot、三个刚性
动作 attachment、EyeFire 骨架、events、slot reset 与 transition mix。把最终 JSON 反向恢复
四类 donor/结构差量并移除新增 cast event 后，与 cast-set 基线语义完全相等。修复前独立审计记录了 432 个
允许叶差、0 个越界叶差；修复后 validator 又把新增 event 作为唯一额外允许差量反向恢复。

## 五页像素契约

五张 PNG 都是已验收供体的逐字节副本，没有重新编码、修改 Alpha 或新增付费调用：

| 输出页 | 供体 | SHA-256 |
| --- | --- | --- |
| `vivhite_combat.png` | `hybrid_neutral_v3` | `FABDB49900D01AF55EF8FF193824149A504019630E1F39AFE692481AE52F6F5B` |
| `vivhite_combat_death.png` | `hybrid_death_v3` | `7C89552C69BB50EB94A3D5A10D4CA408A0546156ED72AA7A289F1CF25A9AF33D` |
| `vivhite_combat_attack.png` | `hybrid_attack_peak` | `17BFEE690FBC7401487CBA0D4E14BA311CDEE8B9A03F1ADA9144FBF4C7114DFB` |
| `vivhite_combat_attack_heavy.png` | `hybrid_action_set` | `E245F4A17D506E97CD34ABD19F55B6C8B884832D01396ECAA837BEAF0A07720E` |
| `vivhite_combat_cast.png` | `hybrid_cast_set` | `65458604F022F0FD98D64737D130F7FC89E621032BF0677D24CCDDEF108B2B5E` |

总装器固定四个 JSON donor、cast wrapper 与五页哈希，并在写入前后重新拍摄全部输入快照；输入
在构建期间变化会直接失败。

最终 Spine JSON SHA-256 为
`608DE3B142BB24A5D2BD402C24B6B1BAD4E643C0896D5931C854BAFE5353AAA1`；TRES SHA-256 为
`EC13C6ED29A38051AED3FABFFFAA5050C79C95F919330719038FAB490594983C`。

## 已完成的精确 Vulkan

总装候选使用游戏匹配的 Spine GDExtension、基础 PCK、Windows display 与 Vulkan 驱动完成：

| 动画 | 样本数 |
| --- | ---: |
| `idle_loop` | 5 |
| `low_health_loop` | 5 |
| `relaxed_loop` | 9 |
| `attack` | 14 |
| `attack_heavy` | 14 |
| `cast` | 14 |
| `hurt` | 7 |
| `die` | 16 |

合计 84/84。每帧检查三个角色 slot 恰好一个可见、期望 attachment、非空 Alpha bbox、画布
四边、mix 和 VFX slot；attack `.08/.20`、heavy `.12/.32`、cast `.25/.60` 与 die `1.05`
均覆盖切换前、切换点和切换后采样。该 renderer 每个样本使用 fresh 状态；连续中断由下节的
同实例状态机证据单独补齐。

## 连续 consumer 的问题与修复

同一 SpineSprite/AnimationState 的 25 条序列覆盖四个热源到六个目标，再加 death→idle；最终
104/104 checkpoint 和 50/50 target t0 / mix+ε 均通过。该结果证明 Spine runtime 会把未在
目标动画 key 的 attachment 恢复 setup，未复现 slash/sigil/人物 slot 残留，因此没有证据支持
批量添加六槽 t0 reset。

真实 C# consumer 给出了不同结论：修复前 8 个 `NIroncladVfx` 场景中 7 个通过，唯一失败是
`cast@.30→cast@0`。新 cast 的 `animation_started` 因名称仍为 cast 不调用清理，旧 EyeFire 在
`.06s` 后仍可见。隔离 A/B 只增加 `clear_vfx@0`；真实事件顺序证明它在 started 之后、首个
epsilon 前隐藏眼火，`.25` 正常重开、`1.222000026` 正常关闭。正式候选复跑 8/8 后采用该修复，
没有引入正式 C# patch。

merchant 使用正式 PackedScene 布局、`.28` 缩放和最终候选，十个 deterministic dirty seek
相位均 body-only，并完成黑、白、游戏绿 SourceOver。standalone 无法绑定 `NMerchantCharacter`
C# 类，报告准确标记为 production layout + seek contract proxy。

## 可复现证据索引

- 总链成功摘要：`.work/hybrid-v3-final/wrapper-full-final-20260829-001049/validation_summary.json`；
- cast 修复后 exact：`.work/hybrid-v3-final/cast-clear0-exact-20260828-154858/`；
- 连续状态机：`.work/hybrid-v3-final-transition-probe-07/transition_summary.json`；
- 真实 VFX：`.work/hybrid-v3-final-runtime-vfx/20260828-234719-147ebec8/evidence/summary.json`；
- merchant：`.work/merchant-vulkan-20260828-152958-5489578/evidence-current/merchant_summary.json`；
- rest-site：`.work/rest-site-acceptance/hybrid-v3-final-regression-20260829-000417-276/report.json`；
- character-select/UI：`.work/character-select-regression-20260829-000559-cfd7a4ef/`；
- multiplayer：`.work/hybrid-v3-final-multiplayer-regression-20260829-001/report.json`。
- no-deploy PCK：`.work/hybrid-v3-final/pck-fixed-20260829-003420-4471698/pck_validation_summary.json`。

这些目录均被 `.gitignore` 排除；长期结论以本文和两份活文档为准。前两次没有 `success=true`
摘要的 wrapper 目录只是 fail-closed 调试证据，不得冒充最终通过。

## 踩坑与约束

- 分项候选不是线性“最后一个覆盖前一个”：neutral、hurt、death 都是不同分支，只能按字段合并。
- `hybrid_death_v3` 少 action bone/slot 上下文；整段复制 die 会丢 action 清理。
- hurt 真正新增的是 `.bones`；整段复制会把 neutral loop reset 回退。
- `.import` 与 `.uid` 是 Godot cache，不属于 8 authored 文件，也不进入 30 文件 runtime。
- `CopyModOnBuild=false` 不会禁 PCK 导出；任何完整 PCK 门禁都必须使用 `.work` 隔离项目并显式
  重定向 `ModPckPath`、`ModOutputDir` 与 `RitsuLibDeployDir`。
- PowerShell `Start-Process -Wait` 会等待 dotnet 的 Roslyn compiler server；隔离 PCK build 必须
  使用 `/p:UseSharedCompilation=false`，不能靠泛杀 dotnet 解除等待。
- `clear_vfx` 的真实 C# 实现只隐藏外部 EyeFire，不会清 Spine slash/sigil，也不会 Kill slash
  Tween。是否需要新增目标动画 t=0 reset，必须先由连续状态机复现，不能凭静态推导扩张改动。

## 下一道门禁

1. 冻结已经通过的严格 30 文件、非战斗回归和完整 PCK 哈希证据；
2. 保持正式 legacy runtime 与游戏 mods 不变；
3. 获得明确部署门禁后才备份旧三件套并进入真实游戏；
4. 真机验收 combat layout、选人、战斗/VFX、商店/伪商人、篝火、UI 与多人资源边界。

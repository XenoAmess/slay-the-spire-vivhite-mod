# 白绮全量美术占位审计与补全

## 初始审计结论（历史基线）

本轮最初同时检查了素材布局和实际 C# / Godot 消费路径。当时活动资源中没有源码直接指向游戏的红色 `NOPE`，但存在大量“路径有效、语义仍是模板占位”的资源：58 张卡牌回退到 RitsuLib 卡图占位，23 个 Power 和孤高冠冕共用旧红色火焰遗物图，白绮能量球及能量标记仍是红色模板。此段只记录补全工作启动时的历史基线，不代表当前运行时完成度。

战斗五页 atlas、商店、休息、选人、顶栏头像、地图标记已经是白绮专属生产资源，不在本轮重画。`images/characters/` 下旧头像、旧选人图和旧地图标记已不被共享 V3 profile 消费，也不应为了“清目录”重复生成。多人四手势继续遵循 2026-08-28 用户批准的封闭运行时例外。

## 消费契约与生成路线

- 卡牌：每张一幅独立、不透明、完整场景图；运行时必须是 `1000×760 RGB8 PNG`。只使用 Codex 原生图像生成，源图经确定性中央 `25:19` 裁切与 Lanczos 缩放；不得走 EvoLink。
- 遗物、Power、能量球：独立透明 UI 消费者；运行时为 `256×256 RGBA PNG`，文本能量标记为 `24×24 RGBA PNG`。只使用 EvoLink `gpt-image-2` 原生透明请求，禁止抠图、色键、蒙版或 Alpha 修补。
- 每次付费透明生成保留原始返回图、完整 Prompt、脱敏请求参数和黑/白/游戏底 SourceOver 验收图。
- 所有图继续执行白绮身份、无武器和嘴部硬禁令。任何红线、猩红丝、液体、血迹或魔力不得接触或从嘴、唇、脸颊附近发出。

## 运行时路由

- `VivhiteCard.CustomPortraitPath` 按编译类型名解析 `images/cards/<CardType>.png`；不再允许 RitsuLib 占位回退。
- `VivhitePowerTemplate.AssetProfile` 按 Power 类型名解析 `images/powers/<PowerType>.png`。四组升级 Power 与基础版共用语义图，不重复生成。
- 孤高冠冕使用 `images/relics/SolitaryCrown.png` 和 `SolitaryCrownOutline.png`；旧 `VivhiteRelic.png` 仅保留为审计历史。

## 卡图生产清单（历史基线）

已有并继续使用：`LuminousProjection`、`ClosedDomainMapping`、`VivhiteTransformation`。

初始审计时待补 58 张；这些条目现均已有独立运行时文件：

- 守恒几何：`AxiomRing`、`ClosedProjection`、`TangentStarlight`、`OpenSetShelter`、`LocalHomeomorphism`、`ScaleTransformation`、`IsoperimetricWard`、`TopologicalGrowth`、`LawOfConservation`、`LifeManifold`、`MobiusLoop`、`Invariant`、`GeodesicVeil`、`ClosedManifold`、`AxiomOfLife`、`InfiniteExtension`、`ConservationFirmament`。
- 递归星算：`RecurrentStarlight`、`TerminationCondition`、`ParallelStarfall`、`AstralSearch`、`HeuristicShield`、`SuccessorFormula`、`BacktrackingSpell`、`ConvergenceVerdict`、`DivideAndConquerCircle`、`AstralPursuit`、`PrefetchFuture`、`InductiveCircle`、`EventLoop`、`ProofOfTermination`、`DynamicProgramming`、`InfiniteStarSequence`、`OptimalAlgorithm`。
- 绯彩积分：`CrimsonArea`、`TrichromaticWaltz`、`CompositeColorWheel`、`DifferentialSampling`、`Chiaroscuro`、`NegativeSpace`、`SpectralIntegral`、`GoldenComposition`、`RiemannStarArray`、`ChromaticTransition`、`ColorConservation`、`CompositeColorField`、`ComplementaryAfterimage`、`DefiniteCrimsonIntegral`、`CrimsonConservationLaw`、`InfiniteCanvas`、`PerfectSynthesis`。
- 跨体系：`GoldenRatio`、`AstralMeasure`、`ChromaticSequence`、`UnifiedFieldTheory`、`ConservedRecurrence`、`ChromaticLimit`、`VivhitesCrimsonTransformationRitual`。

攻击图保持开放、向外、非对称、带明确方向的几何结构；防御/技能图按具体效果使用闭域、屏障或操作性空间结果；能力图保持居中、纵向、自身变换或可持续系统。色彩体系用于区分流派，但不得用统一的红色血线偷懒表示謦欬或汲取。

## 透明资源生产清单

- 孤高冠冕：彩色图与专用轮廓图各一张。
- 19 个 Power 语义图：`AstralPursuitMarginPower`、`AstralPursuitPower`、`ChiaroscuroPower`、`ClosedManifoldPower`、`ColorConservationPower`、`CrimsonConservationLawPower`、`DynamicProgrammingPower`、`InductiveCirclePower`、`InfiniteCanvasPower`、`InfiniteDimensionalityPower`、`InfiniteDrainPower`、`InfiniteDrainThisTurnPower`、`InfiniteExtensionPower`、`InfiniteMarginPower`、`LawOfConservationPower`、`LifeManifoldPower`、`OptimalAlgorithmPower`、`UnifiedFieldTheoryPower`、`VivhitesCrimsonTransformationRitualPower`。
- 能量球五层：现有五个 `Vivhite_energy_orb_layer_*.png` 消费槽位保持独立中心与画布契约。
- 三个内容池共用：`energy_big.png` 与 `energy_text.png`。

## 当前状态

2026-08-31：消费侧审计完成，运行时专属路径已切换，89 项静态运行时资源门禁已经通过；两张历史待重绘卡图也均经独立复核通过，全卡视觉语义 QA 最终达到 `61/61`。完整 PCK 同批部署和 Vulkan 真机卡框验收尚未执行，因此当前状态是“静态资源与静态视觉 QA 已完成，但最终真机运行时验收未完成”。

## 最新只读审计快照（非最终结论）

2026-08-31 最新一次只读审计确认，当前运行时共有 **89 项消费契约**：61 张卡图、19 个 Power 语义图标、孤高冠冕彩色图与轮廓图 2 项，以及 Energy 资源 7 项。

该快照发生在最后两项资源落盘之前，完成度为 **87/89**：

- 卡图：`60/61`。
- Power：`19/19`。
- 孤高冠冕：`2/2`。
- Energy：`6/7`。

快照时仅缺少 `ConservedRecurrence.png`；`energy_text.png` 虽然存在且尺寸、像素格式符合消费契约，但仍是旧红色占位图。除该旧红色 `energy_text.png` 外，其余当时已存在的消费文件之间没有重复哈希，也没有命中已知占位图哈希。

代码中的 23 个具体 Power 类型在将升级类型归并到对应基础语义后，恰好映射为上述 19 个 Power 语义图标。目录中保留的 `VivhiteStrike.png`、`VivhiteDefend.png` 和 `VivhiteRelic.png` 属于旧资源历史，不再被当前运行时代码消费。

此处的 `87/89` 只是一份有明确时间点的只读历史快照，不能冒充当前完成结论。此后 `ConservedRecurrence.png` 与新版 `energy_text.png` 均已落盘，并重新执行了完整静态美术审计；当前结果见下一节。

## 当前静态 Runtime Gate：89/89

2026-08-31 的最新静态运行时审计已经达到 **89/89**：

- 卡图：`61/61`。
- Power 语义图标：`19/19`。
- 孤高冠冕：`2/2`。
- Energy：`7/7`。

完整审计结果为：缺失资源 `0`、命中占位哈希 `0`、重复哈希 `0`。

`energy_text.png` 使用 EvoLink `gpt-image-2` 原生透明路径生成，编号为 `0190`，第 `1/8` 次即通过，未继续消耗重试额度。运行时文件为 `24×24 RGBA8`，四角及四边透明；在 `24px`、`16px`、`12px` 三种实际尺寸下，分别以纯黑、纯白和游戏靛蓝为底执行真实 SourceOver 合成检查，均未发现矩形底、边缘污染或不可辨识问题。该素材没有使用抠图、色键、蒙版或 Alpha 清理，仅在原生透明结果验收后执行确定性尺寸适配和透明留白处理；原图、逐字 Prompt、脱敏请求参数及 SourceOver 检查证据均保留在追加式归档中。

这里的 **89/89** 只证明运行时消费清单中的文件数量、路径、格式、尺寸、Alpha、唯一性及占位哈希门禁全部通过。全卡视觉语义 QA 的最终独立复核结果见下一节；两项静态门禁均不等于完整 PCK 已部署或 Vulkan 真机验收已经完成。

## 全卡视觉 QA：最终 61/61 静态通过

61 张活动卡图已经逐张检查类型辨识、缩略图可读性、武器禁令和嘴部红色元素禁令。此前严格通过 **59/61** 的结果是重绘完成前的明确历史快照；当时武器违规为 `0/61`，嘴边或脸边红色长条、猩红丝线、液体、血迹及口部魔力违规为 `0/61`，但以下两张仍因各自的视觉语义问题进入独立重绘：

- `TopologicalGrowth.png`：卡牌类型为 Skill，但居中人物、持续环绕的多层平面和纵向升华结构更容易被读成 Power，尤其在 `100×76` 灰度缩略图下类型误读明显。
- `DivideAndConquerCircle.png`：Skill 的流程语义清楚，但画面出现醒目的悬浮打开书本。书本虽不是武器，仍违反白绮不依赖书本或施法工具的身份契约。

两张重绘结果现已分别完成独立复核：

- `DivideAndConquerCircle.png`：哈希 `37F19...AA028C`，已通过 Skill 类型辨识、缩略图可读性、无书本/工具、无武器及嘴部禁令复核。
- `TopologicalGrowth.png`：哈希 `93835...3743A5`，已通过 Skill 类型辨识、缩略图可读性、无武器及嘴部禁令复核。

因此，全卡视觉 QA 的最终静态结论为 **61/61 通过**；历史 `59/61` 及上述两项重绘缘由继续保留，不能被最终数字覆盖或误写成从未发生。该结论仍只属于静态检查：完整 PCK 同批部署、游戏内卡框检查和 Vulkan 真机验收均待最终执行，静态通过不等于真机完成。

## NOPE 图标部署诊断

红色 `NOPE` 的已确认原因是已部署 DLL 与 PCK 不属于同一构建批次：DLL 已请求新版独立资源路径，而落后的 PCK 尚未包含对应资源。该问题不是白绮资源注册或消费路径本身错误。静态资源达到 `89/89` 不会自动修复游戏目录中的旧 PCK；仍须完整导出新版 PCK，并将 DLL、PCK 与清单按同一批构建结果一起安装，禁止用新 DLL 搭配旧 PCK。只有部署后日志和真机 UI 均不再出现缺失资源或红色 `NOPE`，该问题才算关闭。

## 修订记录

- 2026-08-31：基于两项最终独立复核，将全卡视觉 QA 从历史快照 `59/61` 更新为最终 `61/61` 静态通过；记录 `DivideAndConquerCircle.png` 哈希 `37F19...AA028C` 与 `TopologicalGrowth.png` 哈希 `93835...3743A5`，并保留两次重绘的原始缘由；确认完整 Runtime Gate 为 `89/89`、缺失 `0`、占位哈希 `0`、重复哈希 `0`；再次明确完整 PCK 同批部署、游戏内卡框与 Vulkan 真机验收仍待执行，静态通过不等于真机完成。
- 2026-08-31：保留 `87/89` 为明确历史快照，追加当前静态 Runtime Gate `89/89`（61 卡图 + 19 Power + 2 孤高冠冕 + 7 Energy）；记录 `energy_text.png` 的 EvoLink `0190` 首次通过及黑、白、游戏靛蓝三底 SourceOver 证据；记录全卡视觉 QA 为 `59/61` 严格通过，以及 `TopologicalGrowth` 类型误读和 `DivideAndConquerCircle` 书本工具两项独立重绘；明确静态门禁不等于 Vulkan 真机完成，`NOPE` 仍须完整 PCK 同批部署后关闭。
- 2026-08-31：保留初始消费侧审计、生成路线与生产清单，追加 `87/89` 最新只读快照；明确 89 项消费契约的组成、Power 类型归并关系、两项未完成资源、旧资源不再消费，以及最终必须重新审计至 `89/89` 的门禁。
- 2026-08-31：补充红色 `NOPE` 的部署撕裂诊断，要求 DLL 与完整 PCK 同批构建、同批部署。

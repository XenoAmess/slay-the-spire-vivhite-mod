# Brain 整体审计与历史压缩（2026-09-12）

本轮目标是减少历史数据与复盘上下文的持续膨胀，修复实际存档故障，并检查决策、持久化、统计展示、复盘和生命周期之间的边界。所有基准均来自本机只读扫描或离线测试，不表示游戏胜率改善。

## 审计基线

当前活动目录约有 1,800 份局档、587 MB 原始 JSON；训练运行中，数量随新局增加。

| 路径/调用 | 修改前实测 |
| --- | --- |
| Ironclad 根目录 | 1,025 份局档，226,942,281 字节 |
| Vivhite Profile | 773 份局档，359,823,535 字节 |
| Ironclad 最近 10 局 | 读取 1,025 份、226,982,997 字节，5.914 秒 |
| Vivhite 最近 10 局 | 读取 773 份、359,823,535 字节，11.806 秒 |
| 楼层统计冷启动 | 全 Profile 约 22.70 秒；热刷新仍扫描约 1,800 个文件 |
| 复盘原始报告 | 根目录 936 KB，Vivhite 242 KB |
| 逐局 lessons | 根目录 1.41 MB，Vivhite 2.11 MB |

当时最近提示文件分别约 149,883 / 127,684 字符，没有宣称每次请求都超预算。预算缺陷来自可复现的超大输入：旧函数只依次裁三次，之后仍可能超过声明预算；被删的失败决策也未同步计数与完整性标签。

所有已扫描的当前原始局档均无代码版本/commit/boot 戳。因此不能把日期等同具体版本，更不能补造某局使用的代码版本。本轮以显式日期区分历史工作集，原始证据仍可精确恢复。

## 架构判断

- 决策由 `agent → client/policy → action receipt` 驱动，学习与终局写盘、异步复盘、驾驶舱分离，方向合理。保留 POST 不透明重放、人工接管排除学习、Profile 隔离和原生存档证据语义。
- `llm_review.py` 约 11,924 行、295 个函数，同时管理上下文、队列、模型、临时仓库、失败包和 Git 合入；`agent.py` 约 4,687 行；`knowledge.py` 约 2,842 行。继续把可独立测试的上下文/历史维护职责提取成小模块，避免一次重写全部运行器。
- `policy.py` 约 9,993 行、`selfcheck.py` 约 13,258 行，存在长期策略迭代累积。此次不代写已委托复盘模型的具体策略成果，不以缩行数替代行为验证。
- 模型上下文、展示用统计与原始局档应分别消费有界摘要、可缓存投影和按需取回的完整 JSON。原始局档不应每次取最近记录时全量解析。

## 已落实的第一批修复

1. `compact_knowledge.py` 增加 `--archive-before YYYY-MM-DD`。历史正常局不再仅因深层/胜利/最长轨迹永久常驻；默认旧参数行为兼容。保留最近 N、进行中、人工接管、排除学习、孤儿、异常和日期不可信记录。
2. 归档扫描排除 `profile_reset_archives`。本机确实存在该备份子树且原扫描会误认其为在线 Profile，不能为了压缩而二次改写统计重置的原始备份。
3. 新 catalog 投影标记版本，保留原始 `profile_run_number`、真实存在的版本/boot 字段，并区分“卡牌归因未记录”和“明确为空”。
4. 最近局读取按文件名倒序，到足够的合格样本便停止；精确批次继续按原始身份查找，缺失批次的近期背景仍明确标记。归档目录可直接按成员 SHA-256 取证，不为每局重复解析整个 manifest。
5. `_save_run_progress` 仅在真实写盘成功后更新 `_rlog_mark`。注入一次 `OSError` 后，相同快照下一轮立即重试，不再误判为刚刚保存而延后到下一批 15 条决策/换层。

首轮相关归档回归 39 项通过；新增历史读取测试 5 项通过；存档重试及原生恢复相关测试 8 项通过。

读取优化后、尚未实施生产归档时，同样取最近 10 局：Ironclad 只读 10 份、3.04 MB、0.1082 秒；Vivhite 只读 11 份（跳过进行中）、4.69 MB、0.1690 秒。耗时受文件缓存与在线负载影响，主要可靠指标是读取文件数和字节数大幅下降。

## 剩余架构债与边界

- `client.py` 的异常 POST 先做串行端口重发现再交回 Agent。虚拟时钟复现最坏链为 30 秒 POST + 5×3 秒发现 = 45 秒，超过统一停止 40 秒协作窗口；这是故障条件下的延迟上界分析，不是当前直播实测停顿。
- 前台每 20 秒的 `refresh_policy` 与自动 Git 网络 push 共用仓库事务锁；慢 push 的 30 秒超时可能阻塞动作循环。已确认源码依赖，尚无本轮当前会话慢 push 的运行证据。后续应把网络推送移出共享锁，而非减少存档校验。
- 原生身份、统计、学习、轮换配额、失败重审亲和性不因历史压缩而重算。直播和训练继续驻留；代码验证与下一次进程实际加载需要分别报告。

## 第二批优化与实际归档

`review_packet.py` 提取纯预算函数，保留批次/profile/run 身份、缺证据状态和补读引用；裁剪说明自身也计入预算。仅保留整条近期决策，并同步 `kept_decisions`、`omitted_decisions`、`serialized_chars` 与完整性标签。未超预算时保持内容完整，不修改调用方输入。926,266 字符合成测试包降至 158,254 字符；这是测试数据，非声称本机实际每次模型输入如此巨大。7 项独立预算测试通过。

`floor_stats.py` 对版本化完整 catalog 投影直接读取，旧投影的身份、选牌与终局卡组共用一次 ZIP 解压解析。缺失证据仍保持缺失；显式空账仍是有效证据。31 项相关测试通过；未压缩同一数据集只读冷启动从 22.70 秒到 19.06 秒，统计值一致。

新增单 store 在线入口 `scripts/compact_run_history.py`，默认只读。必要性是当前直播为 Streaming，训练正在实际操作，而原完整压缩工具要求停栈。新入口仅整理日期边界外的已闭合旧局，长时间压缩在仓库锁外完成；ZIP 回读通过后，短持现有事务锁，以 manifest → catalog/日期摘要 → 精确源文件删除的顺序发布。正常 run 写入并不持有该仓库锁，因此选择近期以外的旧闭合局、发布前与删除前复核源哈希；检测到变化则保留源文件。10 项测试覆盖字节还原、变更保全、损坏 ZIP 和发布中断后的幂等恢复。

2026-09-12 实际执行：每个 Profile 保留最近 96 局，在线归档边界严格早于 **2026-09-09**。结果如下（MB 按十进制）：

| Profile | 归档旧局 | 原文 bytes | ZIP bytes | 保留活动局 |
| --- | ---: | ---: | ---: | ---: |
| Ironclad | 922 | 195,218,894 | 15,406,581 | 103 |
| Vivhite | 407 | 174,400,524 | 14,023,226 | 367 |
| 合计 | 1,329 | 369,619,418 | 29,429,807 | 470 |

原文转为 ZIP 减少 **340,189,611 字节（约 340.2 MB）**，压缩后占原文约 7.96%。此前记录曾把该差额称为净节省，后续补齐索引核算：manifest、catalog 和日期摘要由 666,103 增至 9,411,328 字节，因此工作集实际净减少 **331,444,386 字节（约 331.4 MB）**。这是逻辑文件大小差额，未计文件系统簇分配。新局继续产生，因此后续活动文件计数会上升。旧 Git 对象没有重写，本次节省是工作树空间，不代表仓库历史体积同步减少。

- Ironclad ZIP SHA-256：`9fc7358bd408b3096e25eb2825c056ed758176d73f9085b4c52268c359995e8b`。
- Vivhite ZIP SHA-256：`2c8518a457b89bbeac8a07b8a9164c77c389e0fee458e2715d75647ae1a0a7d8`。
- 发布后重新读取两个 ZIP 的全部 1,329 个 run，逐一核对原始字节数与 SHA-256；再次计划均为 0 个待归档/重复源。
- 在线聚合统计、策略、progression、轮换、队列、active-run、进行中记录、reset 原始备份以及 lessons/meta_review 没有由归档工具改写。仍在线追加的两份 Markdown 原文保留；此次把局档和可读索引整理成冷历史，并通过有界提示视图控制模型上下文。
- 新 catalog 下，根目录 `FloorStatsProvider.snapshot(force=True)` 冷启动实测 **6.97 秒**，Vivhite 单 store **5.16 秒**。完整测量/归档校验摘要见 [证据 JSON](brain-audit-2026-09-12-evidence.json)。

可读历史入口：

- [Ironclad 日期索引](../sts2-ascend/knowledge/archive/history_summary.md)
- [Vivhite 日期索引](../sts2-ascend/knowledge/profiles/vivhite/archive/history_summary.md)

本机命令（从仓库根执行；只读预览时去掉 `--apply`）：

```powershell
$env:PYTHONHOME = 'C:\Users\xenoa\AppData\Local\Programs\Python\Python314'
& C:\Python314\python.exe -X utf8 -B sts2-ascend/scripts/compact_run_history.py --knowledge-dir sts2-ascend/knowledge --archive-before 2026-09-09 --apply
& C:\Python314\python.exe -X utf8 -B sts2-ascend/scripts/compact_run_history.py --knowledge-dir sts2-ascend/knowledge/profiles/vivhite --archive-before 2026-09-09 --apply
# 恢复查看某局原文；只读，不重新写回活动工作集
& C:\Python314\python.exe -X utf8 -B sts2-ascend/brain/compact_knowledge.py --knowledge-dir sts2-ascend/knowledge --show-run 20260822-114652_JRB6NPS12YHV.json
```

## 全量回归发现与修复

- HTTP 初始端口发现可能花费数秒。现在仅在该发现完成后补读已有 F9 暂停状态，避免发现期间用户接管仍发送 POST；已连接的正常动作仍只读一次暂停状态。HTTP 错误响应使用上下文管理，正常错误、畸形 JSON 和截断读取均释放响应。9 项本地模拟传输测试通过，没有向游戏注入测试动作。
- 已绑定的失败复盘批次遇到本轮 provider 启动不可用时，保留原有模型/批次亲和性；显式禁用模型仍按原有路径解除绑定。79 项持久化/复盘队列测试及单项禁用模型迁移检查通过。
- 全量测试曾因旧测试桩遗漏 `_select_review_batch` 的配置参数而进入无限重试。测试桩已同步实际接口，并使意外等待终止测试，避免错误被无限等待掩盖。异步失败包测试先验证保全指针，再执行延迟物化并逐字节验证原文；默认模型断言同步到已启用的 Kimi/Luna 配置，没有改生产配置。
- 日志隔离测试原先比较整个生产日志的哈希/mtime，真实训练同时追加日志会造成假失败。现改为在测试子进程内、导入组件之前安装审计钩子，记录并拒绝该子进程对生产日志的写入尝试，随后验证三个 logger 的隔离路径与实际输出。生产进程继续正常写入，不再全量读取约 210 MB 日志来判断另一进程行为。

本轮直接相关 133 项回归通过；`brain/selfcheck.py` 在最终代码修复后再次输出 `SELFCHECK OK`。最终完整 `unittest discover -s sts2-ascend/tests` 共 **863 项全部通过，0 失败、0 错误、0 跳过，耗时 346.315 秒**。测试覆盖当前共享工作树，也包含已有的并行维护改动；本轮提交只收录本任务的修复及配套验证，不把其他维护源码混入提交。

生产归档期间未执行 Start/Stop/重载，直播姬多次只读核验为 `Streaming`。14:01 同一真实 run 的两个不同 `decision_id` 均取得时间递增的 `applied` 回执，驾驶舱为 `connected`，API 为 `COMBAT`、原生 Profile 1。14:16 复核再次取得另一局的两个时间递增 `applied` 回执及新鲜 `MAP`/Profile 1 快照；Brain PID 39252 仍为原进程。当前进程的代码加载与离线代码测试分开验收；源代码改动在后续正常生命周期重载时生效，归档节省已经生效。完整可复核字段保存在证据 JSON。

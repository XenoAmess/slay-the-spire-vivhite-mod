# Topological Growth visual revision attempt 02 — static assessment

Status: `rejected_crop_unsafe`

## Consumer and format

- Runtime class: `TopologicalGrowth`; intended portrait: `Vivhite/Vivhite/images/cards/TopologicalGrowth.png`.
- The source is one complete opaque `1536×1024` RGB8 scene.
- The deterministic centered `25:19` crop is `x=105, y=8, 1325×1007`, resized with Lanczos to exactly `1000×760 RGB8`.
- This rejected attempt was not copied over the runtime portrait.

## Identity and hard bans

- Exactly one Vivhite is present at screen-left with the established silver hair, violet eyes, round gold glasses, blue butterfly, and white/deep-indigo/violet/gold costume family.
- Both hands are visible, empty, and work over the middle lattice state. Her mouth is naturally closed and unobstructed.
- No weapon, book, wand, staff, held tool, enemy, target, second character, impact, shield, throne, transformation column, vertical ascension, body halo, blood, liquid, mouth-adjacent red/crimson line, strand, ribbon, or mouth-emitted magic appears.

## Skill semantics and four-axis review

- **Primary silhouette:** asymmetric horizontal workstation procedure with three finite states and two connectors; it reads as a one-use Skill rather than a persistent Power.
- **Spatial movement:** state one is compact, state two is locally opened and stretched, and state three is enlarged and sealed.
- **Scene response:** the table reflections follow the finite procedure, while the distant atelier remains ordinary environmental depth rather than an activated field.
- **Target language:** the process modifies a self-resource lattice and ends in Margin; there is no enemy hit, projectile, or defensive enclosure.
- Exact two-arrow topology passes. The final purple-blue crystal is integrated into the third state rather than placed on a fourth pedestal.
- At `100×76` in color and grayscale, the three-state left-to-right procedure remains distinguishable.

## Rejection reason

- The generator still placed the enlarged third state too far to screen-right. After the mandatory centered crop, its outer nodes and embedded Margin crystal are visibly cut by the right edge.
- A critical semantic state therefore remains outside the runtime safe area despite the corrected arrow count and state topology.
- The next attempt changes one spatial assumption: use a smaller third lattice centered around 68% of the frame, embed the crystal in its center sealing socket rather than its outermost node, and reserve the rightmost 22% for architecture only.

## Status

The source, exact prompt, metadata, deterministic crop, full-size grayscale, `250×190`, and `100×76` evidence are retained. Attempt `02/08` is rejected and must never replace the runtime portrait or be used as a visual reference for the next generation.

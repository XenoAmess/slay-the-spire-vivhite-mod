# DynamicProgramming card attempt 01 — static assessment

Status: `static_accepted_pending_game_validation`

## Consumer and file contract

- Runtime class: `DynamicProgramming`.
- Runtime portrait: `Vivhite/Vivhite/images/cards/DynamicProgramming.png`.
- The source is one opaque full-bleed scene illustration, not an atlas, sprite sheet, Spine attachment, transparent VFX layer, or UI icon.
- The native source is `1536×1024 RGB8`; the opacity-enforcing preparation tool accepted every pixel as fully opaque.
- The deterministic centered `25:19` crop is `x=105, y=8, 1325×1007`, resized with Lanczos to an exact `1000×760 RGB8` runtime PNG.
- The archived prepared image and runtime image are byte-identical, both SHA-256 `719068883A9A5168DC242E15616D686D123BDD15158027565ED76ECE7D9F3742`.

## Visual and semantic assessment

- Vivhite reads correctly as the same silver-haired, purple-eyed magical girl with round gold glasses, a blue butterfly ornament, and the white/deep-indigo/violet/gold costume family.
- Her expression and posture are calm and analytical. Both hands are visible and empty; there is no weapon or held prop.
- The scene has distinct foreground crystalline framing, a middle-ground stepped purple-blue state lattice, and deep observatory architecture with receding arches and suspended instruments.
- Multiple incoming star-chart pages illuminate separate state nodes in sequence. The connected paths converge into exactly one faceted core at screen-right.
- The final core is charged but dormant: there is no outgoing projectile, enemy impact, explosion, or current attack beam. The image therefore reads as persistent accumulation reserved for the next attack, not as an attack already occurring.
- The lattice remains open and distributed rather than enclosing Vivhite, so it does not read as a defensive sphere or shield.
- No card frame, UI, readable text, number, equation, pseudo-glyph, logo, signature, watermark, second character, duplicate limb, or cropped critical hand is present.
- Her mouth is naturally closed. There is no red/crimson line, strand, ribbon, liquid, blood, droplet, or mouth-emitted magic near her mouth or face.

## Small-size and grayscale review

- At `250×190`, the calm caster, many lit state nodes, incoming pages, converging paths, and dormant right-side core remain distinct.
- At `100×76`, the dominant silhouette remains “figure + stepped state network + single destination core”; it does not collapse into an attack ray or protective shell.
- In grayscale at both sizes, the state progression and convergence remain readable through value and topology rather than card type color, title, or border.

## Remaining gate

The static source, deterministic crop, full opacity, RGB8 runtime contract, identity, semantic grammar, grayscale, and thumbnail checks pass. Real Vulkan card-frame, hand-size, and enlarged-view validation remain pending and are not claimed by this component.

Attempt 01 is accepted, so generation stops at `1/8`.

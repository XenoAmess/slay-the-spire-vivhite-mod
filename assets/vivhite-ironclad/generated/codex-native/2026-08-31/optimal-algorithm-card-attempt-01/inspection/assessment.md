# OptimalAlgorithm card attempt 01 — static assessment

Status: `static_accepted_pending_game_validation`

## Consumer and file contract

- Runtime class: `OptimalAlgorithm`.
- Runtime portrait: `Vivhite/Vivhite/images/cards/OptimalAlgorithm.png`.
- The source is one opaque full-bleed scene illustration, not an atlas, sprite sheet, Spine attachment, transparent VFX layer, or UI icon.
- The native source is `1537×1023 RGB8`; its 24-bit RGB pixel format contains no Alpha channel.
- The deterministic centered `25:19` crop is `x=106, y=8, 1325×1007`, resized with Lanczos to an exact `1000×760 RGB8` runtime PNG.
- The archived prepared image and runtime image are byte-identical, both SHA-256 `70D34B71CE16B2A49124F644386F5C80DCB0715579927652F8511756AC0A2EF6`.

## Visual and semantic assessment

- Vivhite reads correctly as a single silver-haired, violet-eyed magical girl with round gold glasses, a blue butterfly ornament, and the white/deep-indigo/violet/gold costume family.
- Her expression and open-palmed posture are calm and analytical. Both hands are visible and empty; there is no weapon or held prop.
- The scene has foreground floor machinery and crystal pedestals, a middle-ground optimization hub and reward routes, and deep observatory architecture with receding arches and celestial instruments.
- The upper-left enemy-associated star marker is already dark. Its route leads into the central violet-gold hub instead of depicting a current hit or projectile.
- The hub splits into exactly three stable and spatially separate rewards: a cyan life crystal at lower left, abstract star-chart pages at lower center, and a blue-gold energy crystal at lower right.
- Open concentric tracks, repeated prior nodes, and the vertical crown establish a persistent death-triggered self-power. The system neither encloses Vivhite as a defensive sphere nor fires an attack beam at an enemy.
- No card frame, UI, readable text, number, equation, logo, signature, watermark, second character, duplicate limb, corpse, blood, or gore is present. The abstract linework on the star-chart pages is pictorial star-map geometry, not readable writing or a rule-text substitute.
- Her mouth is naturally closed. There is no red/crimson line, strand, ribbon, liquid, blood, droplet, or mouth-emitted magic near her mouth or face.

## Small-size and grayscale review

- At `250×190`, the extinguished upper-left marker, central hub, and three separate lower reward outputs remain readable.
- At `100×76`, the dominant silhouette remains “calm central operator + persistent orbital engine + three reward channels.” The three outputs remain separate in color and retain distinct value clusters in grayscale.
- The open orbital structure and vertical crown continue to read as a self-power rather than an attack or defensive shell without relying on a card title, type border, or readable text.

## Remaining gate

The static source, deterministic crop, opaque RGB8 runtime contract, identity, ability grammar, grayscale, and thumbnail checks pass. Real Vulkan card-frame, hand-size, and enlarged-view validation remain pending and are not claimed by this component.

Attempt 01 is accepted, so generation stops at `1/8`.

# Topological Growth visual revision attempt 01 — static assessment

Status: `rejected_crop_unsafe`

## Consumer and format

- Runtime class: `TopologicalGrowth`; intended portrait: `Vivhite/Vivhite/images/cards/TopologicalGrowth.png`.
- The source is one complete opaque `1536×1024` RGB8 scene, not an atlas, sprite sheet, transparent cutout, icon, or UI composition.
- The deterministic centered `25:19` crop is `x=105, y=8, 1325×1007`, resized with Lanczos to exactly `1000×760 RGB8`.
- This rejected attempt was not copied over the runtime portrait.

## Identity and hard bans

- Exactly one Vivhite is present at screen-left with short silver hair, violet eyes, round gold glasses, a blue butterfly ornament, and the established white/deep-indigo/violet/gold costume family.
- Both hands are visible, empty, and actively manipulate the finite lattice. Her mouth is naturally closed and unobstructed.
- No weapon, book, wand, staff, held tool, enemy, target, second character, impact, shield, throne, transformation column, vertical ascension, body halo, blood, liquid, mouth-adjacent red/crimson line, strand, ribbon, or mouth-emitted magic appears.

## Skill semantics and four-axis review

- **Primary silhouette:** asymmetric workbench procedure with discrete lattice states; it reads as a Skill rather than a centered persistent Power.
- **Spatial movement:** a finite left-to-right workflow replaces the rejected vertical ascension and orbiting-layer composition.
- **Scene response:** reflections and workstation sockets follow the temporary procedure; the surrounding atelier does not activate as a persistent field.
- **Target language:** the operation targets one finite self-resource lattice and a Margin result; there is no enemy hit, outgoing projectile, or protective shell.
- The three intended lattice stages remain legible in color and grayscale at `100×76`: compact starting mesh, locally opened/stretched mesh, and enlarged sealed mesh.

## Rejection reason

- The generator separated the final Margin crystal into an additional fourth station and inserted a third arrow between the enlarged mesh and the crystal.
- More importantly, the deterministic centered crop cuts off the screen-right side of the terminal Margin crystal. A critical semantic endpoint is therefore outside the runtime safe area.
- The next attempt changes one topology decision: merge the Margin crystal into the third completed/sealed state, keep only two arrows between the three states, and compress the complete workflow into the central crop-safe area.

## Status

The source, exact prompt, metadata, deterministic crop, full-size grayscale, `250×190`, and `100×76` evidence are retained. Attempt `01/08` is rejected and must never replace the runtime portrait or be used as a visual reference for the next generation.

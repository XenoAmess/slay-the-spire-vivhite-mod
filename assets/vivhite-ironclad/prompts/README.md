# Prompt 版本库

本目录保存生成前的 Prompt 版本，当前唯一子目录是 [`evolink/`](evolink/README.md)。Prompt 只是意图和版本证据，不能替代 `generated/` 批次中的逐字请求记录；每次付费调用必须同时保存原图、完整 Prompt 和脱敏实际参数。

Prompt 修改不等于额度重置。一个语义素材最多 8 次付费尝试；达到可用质量立即停止，第 8 次仍不合格就保留全部批次并交由人工评审。
